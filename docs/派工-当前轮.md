# 派工 — 当前轮：批次 4（`ROUND-012` · 小怪 4 帧序列动画 + 地精 4 帧）

> **先读**：`docs/派工-总览.md`（两条正交的轴 + 花名册 + 全局纪律 + "每窗读哪个文件"）。
> **本轮贴窗块不写在这里**：全部在 **`docs/派工-批次4-小怪序列帧.md`**（含【M4】【M7】【M5】三段 + 素材清单 + 播放口径）。本文件只做索引入口，**避免同一份内容两处漂移**。

## 本轮一览

| 窗号（空间身份） | 任务 | 做什么 | 只改这些路径 |
|------|------|--------|--------------|
| **M4**（小怪 / 刷怪） | `TASK-030` | 8 种小怪 4 帧时间驱动动画（10fps / 0.4s 一轮） | `frontend/src/game/enemies/{index.js,constants.js,selftest.mjs}` |
| **M7**（跟班） | `TASK-031` | 地精跟班 4 帧时间驱动动画（口径同上） | `frontend/src/game/companions/{index.js,constants.js,selftest.mjs}` |
| **M5**（世界 / 掉落） | `TASK-032` | **担任 TASK-030/031 的独立验收方**（reviewer ≠ 施工窗） | `.task/TASK-030/verify-report.json`、`.task/TASK-031/verify-report.json`、`.task/TASK-032/evidence/**` |

- 简报原文：`.task/briefs/TASK-030-worker.txt`、`TASK-031-worker.txt`、`TASK-030-verifier.txt`、`TASK-031-verifier.txt`、`TASK-032-worker.txt`
- 设计口径：`docs/P42-内容扩充提案.md` 第 4 节；素材与播放口径：`docs/派工-批次4-小怪序列帧.md`
- **标准开场句**（把整段原样贴给窗口）

```text
我是 M4 窗口。请读 docs/派工-总览.md，再读 docs/派工-批次4-小怪序列帧.md 的【M4】段与 .task/briefs/TASK-030-worker.txt，按环内 斥候→主力→搜剿 执行。
只改规定路径；必须在本窗终端跑 manifest 的 verify_cmd 并贴输出原文；先自证伪（先 FAIL 再 PASS）；不要跑 npm run build、不要打开网页预览、不要自标 done。完工说「M4 已完成，请主导窗口查收」。
```
（M7 把 `M4`→`M7`、`【M4】`→`【M7】`、`TASK-030-worker.txt`→`TASK-031-worker.txt`、`批 次4` 段不变；M5 说「我是 M5 窗口，本轮担任 TASK-030/031 的独立验收方」并读 `TASK-032-worker.txt` + 两份 verifier 简报。）

## 上一轮（批次 3 · `ROUND-011` power 激发力量）已收口

- 状态：四个任务全部 `done`；查收记录见 `docs/RECEIPT-LOG.md` 的 **2026-09-12** 条（含 M7 独立验收的 4 条 findings 与待用户拍板的连射口径）。
- 当时的【M6】【M2】【M3】【M7】贴窗块**已随本轮结束作废**，需要原文可查 git 历史（提交 `d680876` 的上一版 `docs/派工-当前轮.md`）。
