# 派工 — 当前轮（ROUND-011 · P42 批次 3：power 激发力量）

> **怎么用**：你（M1）把下面【M6】/【M2】/【M3】/【M7】四段，各自**原样**贴给对应编号的窗口即可；窗口自己按段内路径读任务书与简报。
> **任务状态唯一来源**：`.task/`（`manifest.json` + `round.json`）；视图见 `docs/TASK-STATUS.md`（禁止手改）。
> **本轮挡位**：能力=bonus（已确认）· **协作=P（人路由多窗，不派子代理）** · 风险按策略表 · `hook_supervision=false`。

## 本轮派工一览

| 窗号 | 任务 | 角色 | 规定路径（只改这些） | 交付物 |
|------|------|------|----------------------|--------|
| **M6** | `TASK-026` power 主体 + 卡牌界面 | 主力（worker） | `frontend/src/ui/constants.js`、`ui/session.js`、`ui/selftest.mjs`、`views/PowerView.vue`、`views/GameShell.vue` | 代码 + `worker-report.json` |
| **M2** | `TASK-027` 连射 / 贯穿强化 / sp-power | 主力（worker） | `frontend/src/game/combat/index.js`、`combat/selftest.mjs`、`game/weapons/index.js` | 代码 + `worker-report.json` |
| **M3** | `TASK-028` 定神（静止 0.3s → 下次必暴） | 主力（worker） | `frontend/src/game/player/index.js`、`player/selftest.mjs` | 代码 + `worker-report.json` |
| **M7** | `TASK-029` 独立验收（搜剿 026/027/028） | 搜剿（verifier） | `.task/TASK-026|027|028/verify-report.json`、`.task/TASK-029/evidence/` | 三份 `verify-report.json` + 独立探针证据 |

**简报文件（原文，窗口可自己读）**
- 施工窗：`.task/briefs/TASK-026-worker.txt`、`TASK-027-worker.txt`、`TASK-028-worker.txt`
- 验收窗：`.task/briefs/TASK-026-verifier.txt`、`TASK-027-verifier.txt`、`TASK-028-verifier.txt`（M7 用）、`.task/briefs/TASK-029-worker.txt`

**设计口径全文**：`docs/P42-内容扩充提案.md` 第 4 节（power 子系统 + 卡牌界面详设）是权威说明。

---

## M6 — power 激发力量主体 + 卡牌界面

**身份句（贴给窗口）**
```text
我是 M6 窗口。请读 docs/派工-当前轮.md 的【M6】段与 .task/briefs/TASK-026-worker.txt，按斥候→主力→搜剿执行。
只改规定路径。必须在本窗终端跑验收命令并贴终端输出原文。不要打开网页预览，不要跑 npm run build（由 M1 集成时统一跑）。
```

- **任务**：`TASK-026`（`.task/TASK-026/manifest.json`）
- **要做**：① power 池/触发（每 10 级）/唯一性与可叠/强制选择 ② 新增 `views/PowerView.vue` 卡牌屏（左进右出循环漂移 → 点击回中 → `scaleX` 翻转揭晓 → 其余卡飘走淡出 → 0.6s 后回对局）③ 回忆记录 + 不计入羁绊
- **对外契约（只调用、不实现）**：授予效果时同时调 `ctx.combat?.applyPower?.(id)`（M2）与 `ctx.player?.applyPower?.(id)`（M3）
- **验收（三条，逐字照跑）**：见 `.task/TASK-026/manifest.json` 的 R1/R2/R3 `verify_cmd`
- **完工汇报**：「M6 已完成，请查收」+ 本窗终端原文

---

## M2 — 连射 / 贯穿强化 / sp-power（战斗侧）

**身份句（贴给窗口）**
```text
我是 M2 窗口。请读 docs/派工-当前轮.md 的【M2】段与 .task/briefs/TASK-027-worker.txt，按斥候→主力→搜剿执行。
只改规定路径。必须在本窗终端跑验收命令并贴终端输出原文。不要打开网页预览，不要跑 npm run build。
```

- **任务**：`TASK-027`
- **要做**：① `applyPower(id)` 入口 ② **连射** 不蓄力 50% / 蓄力 100% 额外一发，额外发**自带 0.75s 冷却**、吃既有加成 ③ **贯穿强化** 穿透 +1、每穿 1 敌本次伤害 +50% ④ **sp-power** 攻击 +20 可叠
- **注意**：`'steady'` 不由你实现（M3），收到时返回 false
- **验收（三条）**：见 `.task/TASK-027/manifest.json` 的 R1/R2/R3 `verify_cmd`

---

## M3 — 定神（静止 0.3s → 下一次攻击必暴）

**身份句（贴给窗口）**
```text
我是 M3 窗口。请读 docs/派工-当前轮.md 的【M3】段与 .task/briefs/TASK-028-worker.txt，按斥候→主力→搜剿执行。
只改规定路径。必须在本窗终端跑验收命令并贴终端输出原文。不要打开网页预览，不要跑 npm run build。
```

- **任务**：`TASK-028`
- **要做**：① `applyPower('steady')` 启用 ② 静止 0.3s 就绪（移动/攻击/受伤即重置）③ `consumeSteadyCrit()` 只吃一次 ④ **不实现暴击本身**（M2 在暴击判定前调用）
- **验收（一条）**：见 `.task/TASK-028/manifest.json` 的 R1 `verify_cmd`

---

## M7 — 独立验收（搜剿 TASK-026/027/028）

**身份句（贴给窗口）**
```text
我是 M7 窗口，本轮是搜剿不是施工。请读 docs/派工-当前轮.md 的【M7】段，以及
.task/briefs/TASK-026-verifier.txt、TASK-027-verifier.txt、TASK-028-verifier.txt。
独立复跑这三个任务的验收命令（本窗终端原文），各写一份 verify-report.json（reviewer 必须是 M7），
并至少给一条你自己写的独立探针证据（不读施工窗断言）。不要改任何业务代码。
```

- **任务**：`TASK-029`
- **要做**：① 本窗逐字复跑 026/027/028 的全部 `verify_cmd`（任一非 0 即 fail）② 各写 `verify-report.json`（`reviewer="M7"`、`worker_window` 分别 M6/M2/M3）③ **至少 1 条挑刺**：自己写探针（示例：连射在冷却内的额外发次数、贯穿强化穿 0/1/2 敌的伤害、定神只吃一次、power 唯一项拿满后池里只剩 sp）④ 证据存 `.task/TASK-029/evidence/`
- **注意**：PowerShell 若是 5.1 不支持 `&&` → 把命令逐字写 `.cmd`（**UTF-8 无 BOM**）用 `cmd /c` 跑

---

## 本轮之后的排队（等用户发话再派）

| 批次 | 内容 | 预期窗口 |
|------|------|----------|
| 批次 3 收口后 | **批次 4**：9 组小怪 4 帧动画（蘑菇怪/蜗牛怪/史莱姆x-1/x-3/裂怪/蝎子怪/毒刺怪/兰花）+ 地精 4 帧 | M4（怪）+ M7（地精）+ 另一个窗搜剿 |
| 可并行 | **图标批次**：出「荆棘 / 滋补 / 击退 / 伴我同行」4 张图标 → 用户过审 → 入 `frontend/public/assets/upgrades/` + `assets/source/upgrades/` | M10（+ 用户过审）|

## 收口规则（M1 唯一）

- 只有 **M1** 能标 `done`、能开游戏实机；工人只交 `worker_done`（独立验收路径由 M7 交 `verifying`/`verified`）。
- 工人跑完**不会**自动交 M1：由用户把「{窗号} 已完成，请查收」送到 M1；M1 **本窗重跑**该窗验收命令才算通过。
- 同一卡点最多 4 次；第 4 次仍 fail → 硬停并写 `docs/BLOCKERS/{窗号}-{短标题}.md`。
