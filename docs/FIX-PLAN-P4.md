# Phase 4 — 图标管线 / 朝向 / 蓄力伤害 / 黑洞

> M1 规格。窗口 **只使用 M 编号**（M1、M4、M5、M8、M10）。禁止 ICON/F 等别名当窗口号。  
> **调整 1**：2026-08-15 查收 pass（用户手绘 11 张）。朝向 / 攻击属性同步 pass。黑洞入池 pass；全图吸入未接线。

## 调整 1（优先）

设置一个独立的生图窗口，为每个升级选项独立的生成选项图标，每次由我检查后在加入游戏中，往后未生成独立图标的先用空白方块图来占据。

| 窗口 | 做什么 |
|------|--------|
| **M8** | 局内空白方块占位；有过审 PNG 则显示 |
| **M10** | 独立出图；默认栅格化 12×12 `ICON_MAPS`；用户采用后拷入 `public/assets/upgrades/` 与 `assets/source/upgrades/` |
| **M1** | 规格、交接文件、查收；不代替 M8/M10 实现 |

画风锁：`docs/ICON-STYLE.md`。禁止把 GenerateImage 插画当默认。

开窗粘贴块以 `docs/HANDOFF-P4-ADJ1.md` 为准（`WINDOW-ASSIGNMENTS.md` 同步副本）。

## 测试 1 → 指派，M1 不改模块

1. **上报 Failed to fetch**：先确认 8080 + MySQL；仍失败再交 **M8** `api.js`。
2. **游侠左右朝向与鼠标相反** → **M4**（`facingFromAngle` 的 `flipX`）。

## 调整 2 → M5

攻击伤害是独立角色属性；未蓄 = attack；满蓄 = attack×2。力量 +8、散射/开眼了 −3 改 attack。

## 新升级 黑洞 → M8 下一环 + 吸结晶接线

选择后吸收全地图经验结晶。图标由 **M10** 生「向内黑洞、静态」；未过审前走 M8 空白方块。
