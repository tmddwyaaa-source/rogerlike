# P35 交付说明 — 局外 UI 重构为阳光草地同世界观

> **轮次**：ROUND-003 · **收口**：TASK-007/008/009/010 全部 `done`（`transition` 收口，每步 Full Gate 通过）
> **状态源**：`.task/` + `docs/TASK-STATUS.md`（禁止手改）
> **编制**：`docs/编制-2026.md` · **任务书**：本文 + `.task/briefs/TASK-00{7,8,9,10}-*.txt`

## 1. 修改文件清单

| 文件 | 改动 | 窗口 |
|------|------|------|
| `frontend/src/ui/pixel.css` | 局外背景回归浅草地；`.rl-root` 恢复原等宽栈（HUD 字体）；`.rl-screen`/`.rl-modal`/`.rl-picker` 用 Zpix；`.rl-panel` 轻量 1px 框；新增 `.rl-opt`（箭头+描边+下压反馈）；`.rl-btn:hover`/`.rl-pick.on:hover`/`.rl-mem-item:hover` 去掉填充变化；删除 `--rl-night*`、`.rl-slab`/`.rl-plaque`、`.rl-ribbon` | M6 |
| `frontend/src/ui/selftest.mjs` | 删除/改写 P34 夜蓝·苔藓绿·石碑断言；新增 P35 断言（字体作用域×2、局外背景=浅草地、轻量框、`.rl-opt`、hover 不变填充、悬停反馈、无渐变圆角模糊、首页横条已删） | M6 |
| `frontend/src/views/StartView.vue` | 首页/选角/难度改为草地直载文字与按钮；删除 `.rl-ribbon` 横条；选角悬停改箭头/描边/下压 | M2 |
| `frontend/src/views/SettingsView.vue` | 设置/测试容器改轻量浅色框；滑条/开关改低对比像素控件（开关不用荧光绿） | M3 |
| `frontend/src/views/MoreView.vue` | 回忆列表改草地上的轻描边记录；详情保留立绘/等级/升级图标/羁绊与悬停 | M3 |
| `frontend/src/views/ResultView.vue` | 结算改草地上紧凑结果区；胜负只用现有文本与克制色差 | M3 |
| `frontend/public/assets/fonts/zpix.woff2` | 保留（P34 引入，随构建输出） | 沿用 |

**未改动（验收核对）**：`frontend/src/views/HudOverlay.vue`、`frontend/src/views/UpgradeView.vue`、`frontend/src/game/**`、`frontend/src/game/match.js`、`backend/**`。

**文档回写**：`游戏当前设计表.md`（对齐日期 2026-09-08，视觉段改 P35）、`docs/GAME-SPEC.md` §7 第 9b 条（P35）、`docs/RECEIPT-LOG.md`（P35 查收条目）。P34 历史文档与任务记录保留，视觉结论由本文替代。

## 2. 六屏截图

| 屏 | 文件 |
|----|------|
| 首页 | `docs/screenshots/ui-redesign/p35-01-menu.png` |
| 选角 | `docs/screenshots/ui-redesign/p35-02-char.png` |
| 难度 | `docs/screenshots/ui-redesign/p35-03-difficulty.png` |
| 设置 | `docs/screenshots/ui-redesign/p35-04-settings.png` |
| 设置（测试面板展开） | `docs/screenshots/ui-redesign/p35-05-settings-test.png` |
| 回忆列表 | `docs/screenshots/ui-redesign/p35-06-memories-list.png` |
| 回忆详情 | `docs/screenshots/ui-redesign/p35-07-memories-detail.png` |
| 结算 | `docs/screenshots/ui-redesign/p35-08-result.png` |

客观审计（`py -3 scripts/ui-audit.py --dir docs/screenshots/ui-redesign --tag p35 --expect grass`）：

```text
PASS p35-01-menu.png        bg=#c5e0a3 grass=0.84 night=0.0000 moss=0.0000
PASS p35-02-char.png        bg=#c5e0a3 grass=0.85 night=0.0010 moss=0.0000
PASS p35-03-difficulty.png  bg=#c5e0a3 grass=0.86 night=0.0000 moss=0.0000
PASS p35-04-settings.png    bg=#c5e0a3 grass=0.72 night=0.0000 moss=0.0001
PASS p35-05-settings-test.png bg=#c5e0a3 grass=0.69 night=0.0000 moss=0.0004
PASS p35-06-memories-list.png bg=#c5e0a3 grass=0.87 night=0.0000 moss=0.0000
PASS p35-07-memories-detail.png bg=#c5e0a3 grass=0.75 night=0.0003 moss=0.0000
PASS p35-08-result.png      bg=#c5e0a3 grass=0.81 night=0.0000 moss=0.0000
RESULT PASS palette-audit
```

## 3. 局内 HUD 截图（证明未受影响）

- 截图：`docs/screenshots/ui-redesign/ingame-09-ingame-hud.png`（headless Edge 实机走一局取帧）
- 字体探针（`node scripts/ui-shot-ingame.mjs --tag ingame`）：

```text
MENU_PROBE {"screenFont":"Zpix, ui-monospace, \"Cascadia Mono\", \"Sarasa Mono SC\", monospace","zpixLoaded":true,"fontFaces":["Zpix:loaded"]}
HUD topbar/botbar/hearts fontFamily = ui-monospace, "Cascadia Mono", "Sarasa Mono SC", monospace
RESULT PASS hud-font-original
```

- 局内画面色板：草地 `#c5e0a3` 0.80 + HUD 遮罩 `#1a2214` 0.12（与 v0.10 一致）。
- 边界核对：`HudOverlay.vue` / `UpgradeView.vue` / `game/**` / `match.js` / `backend` 的 `git status` 为空；pixel.css 中 `.rl-topbar`/`.rl-botbar`/`.rl-hearts`/`.rl-bar`/`.rl-gear`/`.rl-bonds`/`.rl-bond-tip`/`.rl-frame--dark` 及 `282px`/`flex-wrap`/`pointer-events: auto`/`margin-top: -26px`/`62%` 全部在位。

## 4. selftest 与 build 原始输出

```text
$ cd frontend && node src/ui/selftest.mjs
PASS  音量持久化
PASS  memory append

RESULT PASS

$ cd frontend && npm run build
computing gzip size...
dist/index.html                   0.41 kB │ gzip:  0.29 kB
dist/assets/index-PoBPwELa.css   24.06 kB │ gzip:  4.08 kB
dist/assets/index-CzBpshqt.js   189.96 kB │ gzip: 68.53 kB
✓ built in 487ms
```

M1 本窗复跑的三窗 verify（13 条）全部 exit 0，原文见 `.task/TASK-010/evidence/m1-rerun-p35.txt`。

## 5. P34 清理项逐条说明（7/7）

| # | 清理项 | 结论 | 证据 |
|---|--------|------|------|
| 1 | 删除局外 `.rl-screen` 夜蓝背景覆盖 | ✅ | 两条 `.rl-screen` 规则均无 `#1b202b/#12161f/#232a38`；末条 `background: var(--rl-screen)` |
| 2 | 移除 `--rl-night`/`--rl-night-deep`/`--rl-night-lift` 落点 | ✅ | `grep --rl-night* pixel.css` = 0 处 |
| 3 | 删除 `.rl-glyph-btn:hover` 亮绿填充与反色文字 | ✅ | 规则体仅 `transform: translateY(1px)` |
| 4 | 删除标题石牌/厚重双层深色框 | ✅ | `.rl-slab` / `.rl-plaque` = 0；`.rl-ribbon` 深色横条规则整段删除 |
| 5 | 清理首页底部横条装饰 | ✅ | `pixel.css` 与 `StartView.vue` 均无 `rl-ribbon`；selftest 有对应断言 |
| 6 | 修正字体作用域（Zpix 只作用局外，HUD 不继承） | ✅ | `.rl-root` 保留等宽栈且无 Zpix；局内探针 `hud-font-original` |
| 7 | 更新 selftest 断言为 P35 真实规则 | ✅ | P34 关键词 6 行全为否定式；新增 P35 断言；M6 负向探针 5/5 被捕获（非恒真） |

## 6. 已知说明

- **`read_image` 在本会话不可用**（当前模型不接受图像输入），视觉复核采用可复核替代：headless Edge 实机截图 + 像素级色板审计 + Windows OCR 文本核对 + 计算样式探针。建议用户实机确认观感（尤其悬停箭头与 1px 下压的动效）。
- 代码未提交、未推送；`main` 仍为 `5f11e90`（v0.10）。
