# 派工 — 批次 4：小怪 4 帧序列动画（窗口入口）

> **先读**：`docs/派工-总览.md`（两条正交的轴 + 花名册 + 全局纪律）。
> **本轮状态**：已开工（`ROUND-012`，`TASK-030`/`TASK-031`/`TASK-032` 均 `in_progress`）；**36 张帧图已由 M1 双拷落盘**，现在可以直接开 M4 / M7 / M5 三个窗。

## 素材（用户提供）

- 来源目录：`C:\Users\user\Desktop\Roger-png\小怪`
- 内容：**8 种怪 + 地精**，每组 **4 帧**，命名 `X.png` / `X-2.png` / `X-3.png` / `X-4.png`，**全部 32×32**
- 名单：蘑菇怪、蜗牛怪、史莱姆x-1、史莱姆x-3、裂怪、蝎子怪、毒刺怪、兰花 → **怪（M4）**；**地精 → 跟班（M7）**
- 注：现在运行时目录里只有各怪的**第 1 帧**（旧单帧已被覆盖成新画风），`-2/-3/-4` 三帧只在桌面目录。
- **落盘已完成（M1）**：`frontend/public/assets/小怪/`（8 怪 ×4 帧）+ `frontend/public/assets/跟班/`（地精 ×4 帧），并同名备份到 `assets/source/`；两处已逐字节校验一致（地精只放 `跟班/`，不留在 `小怪/`）。

## 播放口径（两窗共用，务必一致）

- **时间驱动**，与角色 `frontend/src/game/render/ranger.js` 的 `ANIM_FPS` 同一套口径；**禁止逐帧计数**（那会随帧率变化）。
- 帧时长 **0.1s ≈ 10fps**，**0.4s 一轮**，无限循环。
- 帧索引对帧数取模；**越界与负时间兜底**，不崩。
- 每只怪/每只地精的**相位错开**（不要全场同一帧）。
- 缺帧/加载失败 → **退化为单帧不崩**。
- `playing` 以外（暂停/升级）**动画一并停住**。

---

## M4 — 8 种小怪 4 帧序列动画

**贴给窗口的话**
```text
我是 M4 窗口。请读 docs/派工-总览.md 与 docs/派工-批次4-小怪序列帧.md 的【M4】段，以及 .task/briefs/TASK-030-worker.txt。
任务 TASK-030：只改 frontend/src/game/enemies/{index.js,constants.js,selftest.mjs}。
按环内 斥候→主力→搜剿 执行（三阶段依次走，不是长期身份）。
必须在本窗终端跑 manifest 的 verify_cmd 并贴输出原文（PowerShell 5.1 不支持 &&，请写 UTF-8 无 BOM 的 .cmd 用 cmd /c 跑）。
先自证伪：新断言先写、实现未加时确认 FAIL，再加实现转 PASS，两次都留证据。
不要跑 npm run build，不要打开网页预览，不要自标 done。完工说「M4 已完成，请主导窗口查收」。
```

- **任务**：`TASK-030`（`.task/TASK-030/manifest.json`）
- **规定路径**：`frontend/src/game/enemies/index.js`、`enemies/constants.js`、`enemies/selftest.mjs`
- **要做**：8 种怪各 4 帧时间驱动循环（10fps / 0.4s 一轮）；取帧做成**纯函数**导出便于断言；`CREEP_FRAMES` 按素材改 **4**；相位按实例错开；缺帧兜底；**不影响 hurtbox/护甲描边/精英变体/死亡掉落**；暂停时停住
- **验收（两条，逐字照跑）**：见 `.task/TASK-030/manifest.json` 的 R1/R2 `verify_cmd`
- **不要碰**：`game/companions/**`（地精归 M7）、素材文件（M1 落盘）

---

## M7 — 地精跟班 4 帧序列动画

**贴给窗口的话**
```text
我是 M7 窗口。请读 docs/派工-总览.md 与 docs/派工-批次4-小怪序列帧.md 的【M7】段，以及 .task/briefs/TASK-031-worker.txt。
任务 TASK-031：只改 frontend/src/game/companions/{index.js,constants.js,selftest.mjs}。
按环内 斥候→主力→搜剿 执行。必须在本窗终端跑 manifest 的 verify_cmd 并贴输出原文（UTF-8 无 BOM 的 .cmd + cmd /c）。
先自证伪（先 FAIL 再 PASS，两次留证）。不要跑 npm run build，不要打开网页预览，不要自标 done。
完工说「M7 已完成，请主导窗口查收」。
```

- **任务**：`TASK-031`
- **规定路径**：`frontend/src/game/companions/index.js`、`companions/constants.js`、`companions/selftest.mjs`
- **要做**：地精 4 帧时间驱动循环（口径同上）；**多只地精相位错开**；**不影响**索敌/软拉绳跟随/碰撞伤害/加伤移速/暂停冻结
- **验收（一条，逐字照跑）**：见 `.task/TASK-031/manifest.json` 的 R1 `verify_cmd`
- **前置**：M1 已把 `跟班/地精{-2,-3,-4}.png` 双拷落盘

---

## M5 — 担任 TASK-030/031 的独立验收方

**贴给窗口的话**
```text
我是 M5 窗口。本轮在 TASK-030/031 上担任「独立验收方」——这是任务层面的约束（reviewer 必须 ≠ 实现该任务的窗口），不是我的长期身份。
我这一环同样按 斥候 → 主力 → 搜剿 走：先只读摸清 M4/M7 的改动与验收命令，再写两份 verify-report.json（reviewer="M5"）与我自己的独立探针，最后对自己的结论挑刺。
请读 docs/派工-总览.md 与 docs/派工-批次4-小怪序列帧.md 的【M5】段，以及 .task/briefs/TASK-030-verifier.txt、TASK-031-verifier.txt 与 .task/briefs/TASK-032-worker.txt。
不要改任何业务代码，不要打开网页预览。
```

- **任务**：`TASK-032`
- **允许写的文件**：`.task/TASK-030/verify-report.json`、`.task/TASK-031/verify-report.json`、`.task/TASK-032/evidence/**`
- **要做**：① 本窗逐字复跑 TASK-030/031 的全部 `verify_cmd`（任一非 0 即 fail）② 写两份 `verify-report.json`（`reviewer="M5"`、`worker_window` 分别 M4 / M7）③ 自己写独立探针：36 张帧图的尺寸/帧数与**两处目录逐字节一致**、取帧函数边界（t=0/0.1/0.4/-0.1/超大 t）、动画不改变既有判定 ④ `findings` 里各写至少 1 条挑刺或边界结论
- **验收（三条）**：见 `.task/TASK-032/manifest.json` 的 R1/R2/R3 `verify_cmd`

---

## 收口（M1 唯一）

1. M4、M7 完工 → 用户把「M4/M7 已完成，请查收」送到 M1
2. M5 独立验收完 → 用户把「M5 已完成，请查收」送到 M1
3. M1：本窗重跑所有 `verify_cmd` + 磁盘核对 → `transition` 收口 → `audit-round` → 回写 `游戏当前设计表.md` / `docs/GAME-SPEC.md`（小怪动画口径）→ 提交 → 开游戏给用户实机
