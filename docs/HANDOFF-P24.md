# 交接 — P24（羁绊改版：天行健文案 / 万物一心 2-4-6-8 新效果 / 新增小金刚）

> **主导**：M1  
> **日期**：2026-08-23  
> **本环窗口**：M8、M11。优先目标接线由 **M1** 做。  
> 子窗口禁止标 Registry `done`。禁止打开游戏。完工说：`M{n} 已完成，请主导窗口查收。`

环内：**斥候 → 主力 → 搜剿**；同一卡点最多 4 次。

已拍板（用户 2026-08-23）：

- **天行健**悬停档位文案细化，写明「随等级提高」：档 2 随等级提高移速（每 2 级 +0.01）；档 4 随等级提高伤害（每 2 级 +1）；档 6 随等级提高空血上限（每 10 级 +1）；档 8 随等级提高穿透（每 15 级 +1）。
- **万物一心**档位 **3/6/9 → 2/4/6/8**，效果全部更换：档 2 跟班伤害 +5；档 4 跟班获得角色当前伤害 20%；档 6 跟班攻击单位 +1；档 8 跟班移速 +0.2、跟班伤害 +10、**优先攻击角色正在攻击的目标**。悬停档位说明可精简。
- **新增羁绊「小金刚」**：目前无效果；计入升级为**大娃、黑洞**；两种都选过即触发（档 2），右侧可见；效果文本显示**「敬请期待」**。

---

## 窗口分工

| 编号 | 本环职责 | 规定路径 |
|------|----------|----------|
| **M8** | 天行健/万物一心 tooltip 文案；万物一心阈值 2/4/6/8；小金刚羁绊数据与 chip | `frontend/src/ui/**`、`frontend/src/views/**` |
| **M11** | `setUnityTier` 新档位效果（含移速/加伤/优先目标） | `frontend/src/game/companions/**` |
| **M1** | `match.js` 优先目标接线（`getPriorityTarget`）、查收联调 | `docs/**`、`match.js`、设计表 |

不要改 `combat/**`、`player/**`、`world/**`、`enemies/**`、`spawner/**`、`App.vue`、`backend/**`。不要生图。

---

## 1. M8 — 文案 / 阈值 / 小金刚

### 1.1 天行健 tooltip 档位文案（细化，机制不变）

- 档 2 · **随等级提高移速**（每 2 级 +0.01）
- 档 4 · **随等级提高伤害**（每 2 级 +1）
- 档 6 · **随等级提高空血上限**（每 10 级 +1）
- 档 8 · **随等级提高穿透**（每 15 级 +1）

羁绊说明行可写「不同种类升级集齐解锁档位，按角色等级补发」。

### 1.2 万物一心

- 阈值 **3/6/9 → 2/4/6/8**（档位判定、`setUnityTier` 传值同步）。
- 悬停档位说明**精简**（机制细节交给 M11 数值，例如）：
  - 档 2 · 跟班伤害 +5
  - 档 4 · 跟班获得角色伤害 20%
  - 档 6 · 跟班攻击单位 +1
  - 档 8 · 跟班移速 +0.2；伤害 +10；优先攻击角色的目标

### 1.3 新增小金刚（id 自定，如 `vajra`）

- `UPGRADES`：`giant`（大娃）、`blackhole`（黑洞）补 `bond` 字段指向小金刚。
- 阈值只有 **档 2**（两种都选过即达）；档 2 前不显示 chip。
- chip：文本「小金刚」；悬停效果文本**「敬请期待」**（可带 档 2 · 敬请期待）。机制上无任何数值效果。
- `session.bonds` 快照与右侧渲染走现有羁绊机制。
- ⚠️ 「敬请期待」是 StartView 断言的**禁串**——只允许出现在羁绊文案，不要进 StartView。

### 1.4 selftest

万物一心新阈值；天行健新文案关键词（如「随等级提高」）；小金刚 bond 字段与 chip；既有断言保留（`rl-bonds` 等）。

`node src/ui/selftest.mjs` 全绿。

---

## 2. M11 — setUnityTier 新效果

档位效果按 **2/4/6/8** 覆盖（可重复调用按当前档覆盖，不叠加，沿用 P18 规则）：

| 档 | 效果 |
|----|------|
| 2 | 所有跟班伤害 **+5** |
| 4 | 每次跟班伤害再加 **floor(角色当前攻击 × 0.2)** |
| 6 | 地精/兔/蝙蝠/蛋可打目标 **全部 +1**（蛋 2~3 仍须重叠） |
| 8 | 全跟班移速 **+0.2 设计单位**（+16 px/s）；跟班伤害再 **+10**；索敌**优先 `opts.getPriorityTarget?.()` 返回的活目标**（不在 targets 或已死则回退常规索敌）；适用全部跟班（含兔子的最近索敌——档 8 时优先目标压过「离自己最近」） |

- 优先目标接口名 M11 自定（如 `opts.getPriorityTarget`），M1 按实现接线。
- 没档 = 全 0。

`node src/game/companions/selftest.mjs` 全绿（新档位数值、优先目标命中与回退）。

---

## 3. M1 接线（子窗口完工后）

- `match.js`：利用现有 `onDamage`（玩家伤害数字通道）记录**玩家最近击中的活敌**，`createCompanions({ ..., getPriorityTarget })` 传入（接口名以 M11 为准；目标死亡/不在 targets 由 M11 回退）。
- 查收：全模块 selftest + build + 开游戏（无敌+自选快速验证：万物一心档位、小金刚 chip、优先目标跟随玩家攻击目标）。
- 回写设计表 / GAME-SPEC。

---

## 成功标准

| 窗 | 标准 |
|----|------|
| M8 | 天行健细化文案；万物一心 2/4/6/8；小金刚 chip「敬请期待」；ui selftest 绿 |
| M11 | 新档位数值（+5 / 20% / +1 目标 / +0.2+10+优先）；companions selftest 绿 |
| M1 | getPriorityTarget 接线、全绿 build、实机三羁绊验证、文档回写 |

---

## 开工粘贴块

### M8

```text
@multi-window_M @game-developer
我是 M8 窗口。项目路径：D:\Cursor_projectt\rogerlike
请读 docs/HANDOFF-P24.md。当前角色：先斥候，通过后再主力。
只改：frontend/src/ui/** 、frontend/src/views/**
不要改 combat / player / world / companions / match.js / App.vue / backend。不要标 Registry done。
不要打开游戏。不要生图。

本环（P24 羁绊）：
1) 天行健悬停档位文案细化：档2 随等级提高移速（每 2 级 +0.01）；档4 随等级提高伤害（每 2 级 +1）；档6 随等级提高空血上限（每 10 级 +1）；档8 随等级提高穿透（每 15 级 +1）。
2) 万物一心阈值 3/6/9 → 2/4/6/8（判定与 setUnityTier 传值同步）；档位说明精简：跟班伤害+5 / 跟班获得角色伤害20% / 攻击单位+1 / 移速+0.2、伤害+10、优先攻击角色的目标。
3) 新增小金刚羁绊：大娃、黑洞补 bond 字段；只有档 2（两种都选过）；chip「小金刚」，悬停效果文本「敬请期待」；无数值效果。注意「敬请期待」禁串只限 StartView。
更新 ui/selftest.mjs。

完成后说：M8 已完成，请主导窗口查收。
```

### M11

```text
@multi-window_M @game-developer
我是 M11 窗口。项目路径：D:\Cursor_projectt\rogerlike
请读 docs/HANDOFF-P24.md。当前角色：先斥候，通过后再主力。
只改：frontend/src/game/companions/**
不要改 ui / enemies / match.js / player / combat。不要标 Registry done。不要打开游戏。

本环：setUnityTier 档位改 2/4/6/8：2=跟班伤+5；4=再+floor(攻击×0.2)；6=可打目标+1；8=移速+0.2 设计单位、伤害再+10、索敌优先 opts.getPriorityTarget?.() 的活目标（失效回退常规，含兔子）。可重复调用按档覆盖。
更新 companions/selftest.mjs（数值、优先命中与回退）。

完成后说：M11 已完成，请主导窗口查收。
```
