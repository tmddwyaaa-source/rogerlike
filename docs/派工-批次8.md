# 派工 — 批次 8（`ROUND-016`）：回血飘字 / 狼群 / 战士激发力量三条 / 弹反接口 / 分池与文案

> **先读**：`docs/派工-总览.md`（两条正交的轴 + 花名册 + 全局纪律）。
> **六个窗可开**：**M3 / M7 / M2 / M4 / M6**（施工）+ **M5**（独立验收方）。素材 M1 已核规格（见下）。

## 用户口径（已逐条确认）

| # | 口径 |
|---|---|
| 1 | **所有回血都要飘数字**（像蝙蝠那样）：恢复 / 滋补 / 生生不息档 5 / 果实 / **生存（+1 血）也要飘**；**档 8 复活不飘**；一次回血只能飘一次 |
| 2 | 新普通跟班 **狼群**：一次生成 **3 只狼**，每只**基础伤害 4**；AI 与行为**完全复用小恶魔**（离角色 ≤3 身位取最近、3.5 身位松手、2 身位舒适环、软拉绳）；**计入万物一心** |
| 3 | 战士激发力量也「每 10 级四选一」：池 = **斩返 / 旋风斩 / 壁垒 / sp-power**（前三个各限一次）；**法师暂时仍只有 sp** |
| 3a | **斩返**：挥砍攻击范围 **+1 身位**（**最后结算、只相加、不被相乘**）+ 挥砍**弹反子弹（子弹消失）CD 0.5s**；**不加厚度、不改角度** |
| 3b | **旋风斩**：额外一发（不蓄力 50% / 满蓄 100%、晚 0.2s、自带 0.75s 冷却从主发触发起算、不影响主发间隔）→ 效果 = **以角色为中心的圆形、半径＝当前挥砍长度**、范围内**每个敌人各结算一次**、**伤害 70% 且只吃暴击与暴击伤害** |
| 3c | **壁垒**：角色**挥砍命中怪物**每 **400** 次 +1 护甲（满 400 清零继续累计；不算跟班 / 葫芦娃 / 羁绊 / 荆棘 / 脉冲） |

## 素材（M1 已核）

| 文件 | 规格 | 结论 |
|---|---|---|
| `upgrades/wolf_pack.png` | **48×48、不透明底** | ✅ 与既有图标同规格（运行时 + `assets/source/` 两处一致） |
| `跟班/狼群.png` + `狼群-2/-3/-4.png` | **52×57**、透明底、4 帧**互不相同**（相邻帧差约 1.1k 像素） | ✅ 可用；跟班统一按 `GOBLIN_DRAW`(16) 绘制，源图非正方形会有**轻微纵向拉伸**，若实机看着别扭再让用户出方形图 |

---

## 【M3】回血飘字统一到 `player.heal()`

```text
我是 M3 窗口。请读 docs/派工-总览.md 与 docs/派工-批次8.md 的【M3】段，以及 .task/briefs/TASK-052-worker.txt。
任务 TASK-052：只改 frontend/src/game/player/{index.js,selftest.mjs}。
按环内 斥候→主力→搜剿 执行。必须在本窗终端跑 manifest 的 verify_cmd 并贴输出原文（UTF-8 无 BOM 的 .cmd + cmd /c，含 % 写 %%）。
tests[] 只放可执行命令（不要 # 注释 / bash 花括号 / 管道 / 裸 % / npm run build），说明写 notes。先自证伪。
不要打开网页预览、不要自标 done。完工说「M3 已完成，请主导窗口查收」。
```

---

## 【M7】狼群（3 只 × 基础伤 4，复用小恶魔 AI）

```text
我是 M7 窗口。请读 docs/派工-总览.md 与 docs/派工-批次8.md 的【M7】段，以及 .task/briefs/TASK-053-worker.txt。
任务 TASK-053：只改 frontend/src/game/companions/{index.js,constants.js,selftest.mjs}。
按环内 斥候→主力→搜剿 执行。必须在本窗终端跑 manifest 的 verify_cmd 并贴输出原文（UTF-8 无 BOM 的 .cmd + cmd /c）。
tests[] 只放可执行命令。先自证伪。不要打开网页预览、不要自标 done。完工说「M7 已完成，请主导窗口查收」。
```

- 升级 id 统一用 **`wolf_pack`**（入池与文案由 M6 的 TASK-056 做）；你只做 `companions/**` 的行为 + 4 帧动画。

---

## 【M2】战士激发力量三条

```text
我是 M2 窗口。请读 docs/派工-总览.md 与 docs/派工-批次8.md 的【M2】段，以及 .task/briefs/TASK-054-worker.txt。
任务 TASK-054：只改 frontend/src/game/{combat/index.js,combat/selftest.mjs,weapons/index.js}。
按环内 斥候→主力→搜剿 执行。必须在本窗终端跑 manifest 的 verify_cmd 并贴输出原文（UTF-8 无 BOM 的 .cmd + cmd /c）。
tests[] 只放可执行命令。先自证伪。不要打开网页预览、不要自标 done。完工说「M2 已完成，请主导窗口查收」。
```

- power id 统一：**`slash_return` / `whirlwind` / `bulwark`**（M6 的池子用同一套 id）。
- 弹反的**执行**在 M1（`match.js` 注入 hook → M4 的 `TASK-055` 清除弹体）；你只判定 + 调 hook。

---

## 【M4】敌方弹体「被弹反即消失」接口

```text
我是 M4 窗口。请读 docs/派工-总览.md 与 docs/派工-批次8.md 的【M4】段，以及 .task/briefs/TASK-055-worker.txt。
任务 TASK-055：只改 frontend/src/game/enemies/{index.js,constants.js,selftest.mjs}。
按环内 斥候→主力→搜剿 执行。必须在本窗终端跑 manifest 的 verify_cmd 并贴输出原文（UTF-8 无 BOM 的 .cmd + cmd /c）。
tests[] 只放可执行命令。先自证伪。不要打开网页预览、不要自标 done。完工说「M4 已完成，请主导窗口查收」。
```

---

## 【M6】战士 power 池（4 选 1）+ 狼群入池

```text
我是 M6 窗口。请读 docs/派工-总览.md 与 docs/派工-批次8.md 的【M6】段，以及 .task/briefs/TASK-056-worker.txt。
任务 TASK-056：只改 frontend/src/ui/{constants.js,session.js,selftest.mjs}。
按环内 斥候→主力→搜剿 执行。必须在本窗终端跑 manifest 的 verify_cmd 并贴输出原文（UTF-8 无 BOM 的 .cmd + cmd /c）。
tests[] 只放可执行命令。先自证伪。不要打开网页预览、不要自标 done。完工说「M6 已完成，请主导窗口查收」。
```

- **注意**：战士三条 power 的**文案要短**（卡面不溢出，一句关键能力）；原断言「power 池按角色」要改成分池结果（游侠 4 / 战士 4 / 法师 1）。

---

## 【M5】独立验收方（TASK-052…056）

```text
我是 M5 窗口。本轮担任 TASK-052/053/054/055/056 的「独立验收方」（reviewer 必须 ≠ 施工窗），这不是我的长期身份。
我这一环同样按 斥候 → 主力 → 搜剿 走。请读 docs/派工-总览.md 与 docs/派工-批次8.md 的【M5】段，以及 .task/briefs/TASK-057-worker.txt。
不要改任何业务代码，不要打开网页预览。
```

- 五份 `verify-report.json`（`reviewer="M5"`）+ 逐字复跑 + 自写探针（见 manifest R3 清单）。
- ⚠️ `changed_files` **必须申报自己 `allowed_paths` 内的全部新增文件**（五份 verify-report + evidence），否则门禁判「越界未申报」（前几轮踩过）。

---

## M1 自己的活（收口阶段）

1. **素材入库**：`wolf_pack.png` + `狼群*.png` 两处目录已一致 → 提交进 git。
2. **`match.js` 接线**：① 弹反——把 M4 的「区域内清除敌方弹体」注入给 combat 的 hook；② 回血去重——蝙蝠链（`companions.tryHeal → hooks.onHeal → match.onHeal(player,n)`）不再重复飘字（飘字已收进 `player.heal()`）。
3. 查收、本窗全量重跑、文档回写（设计表 / SPEC / 更新日志 v0.15）、发布、开游戏。
