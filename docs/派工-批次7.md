# 派工 — 批次 7（`ROUND-015`）：蝎子弹拆分 / 树木去序列帧 / 荆棘特效 / 小金刚改法 / 生生不息档8 / 贪婪

> **先读**：`docs/派工-总览.md`（两条正交的轴 + 花名册 + 全局纪律）。
> **素材已由 M1 落盘并核过规格**（见下），六个窗现在可开：**M4 / M5 / M2 / M3 / M6**（施工）+ **M7**（独立验收方）。

## 用户口径（已逐条确认）

| # | 口径 |
|---|---|
| 1 | **冰锥只属于 Boss 冰人**；蝎子怪的子弹**回归**旧贴图（`子弹/怪物子弹.png`），两者彻底拆开 |
| 2 | 新高级升级 **贪婪**：获取经验时 **50%** 概率再获得一次经验，**0.1s 冷却**，每层 **+10%** 概率，**上限 100%**；**不计入任何羁绊** |
| 3 | 小金刚强化改为「**再次获得一次七兄弟升级选项的效果**」（＝该兄弟按**层数 +1** 生效）；七色脉冲 **1.5 → 3 身位**、伤害 **×1.3 → ×2**；羁绊文本同步 |
| 4 | 生生不息新增**档 8**：受致命伤**以 1 血复活**，冷却＝复活后**再回血 5 次**（5 次事件，不是 5 滴血）。**当前没有触发机会**（只计 5 种升级，凑不到 8 档）——先按要求把效果与接口做好 |
| 5 | 荆棘 / 滋补 图标 + 荆棘 4 帧爆发特效素材（用户提供，M1 已核规格并入库） |
| 6 | **移除树木的序列帧**（灰树 + 普通树/浅树回单帧）；小怪 / 冰人 / 跟班动画保留 |

## 素材（M1 已落盘 + 已核规格，两处目录一致）

| 文件 | 规格 | 备注 |
|---|---|---|
| `upgrades/thorn.png`、`upgrades/nourish.png` | 48×48，**不透明底**（与既有图标一致） | 会被现有图标加载机制自动识别，**不需要改代码** |
| `fx/thorn-burst-1..4.png` | 96×96，**透明底**（1 帧 528 不透明像素；2～4 帧为半透明＝逐层淡出） | 供 M2 在荆棘触发时播放 |

（M1 已 `git add` 入库：运行时 `frontend/public/assets/{upgrades,fx}/` + 备份 `assets/source/{upgrades,fx}/`。）

---

## 【M4】蝎子怪子弹回归 + 移除灰树序列帧

**贴给窗口的话**
```text
我是 M4 窗口。请读 docs/派工-总览.md 与 docs/派工-批次7.md 的【M4】段，以及 .task/briefs/TASK-046-worker.txt。
任务 TASK-046：只改 frontend/src/game/enemies/{index.js,constants.js,selftest.mjs}。
按环内 斥候→主力→搜剿 执行。必须在本窗终端跑 manifest 的 verify_cmd 并贴输出原文（UTF-8 无 BOM 的 .cmd + cmd /c，含 % 写 %%）。
tests[] 只放可执行命令（不要 # 注释 / bash 花括号 / 管道 / 裸 % / npm run build），说明写 notes。
先自证伪（先 FAIL 再 PASS）。不要打开网页预览、不要自标 done。完工说「M4 已完成，请主导窗口查收」。
```

- **要做**：① 蝎子怪的弹体改回 `子弹/怪物子弹.png`（旧观感、不加尖头补偿），与冰人的冰锥**彻底分开**（贴图/尺寸/朝向各自独立）；② **灰树回单帧**（`GRAY_FRAMES` → 1，删掉 4 帧取帧与预加载），自损/百分比血/裂怪流程不变；素材文件不要删。

---

## 【M5】移除普通树（浅树）序列帧

**贴给窗口的话**
```text
我是 M5 窗口。请读 docs/派工-总览.md 与 docs/派工-批次7.md 的【M5】段，以及 .task/briefs/TASK-047-worker.txt。
任务 TASK-047：只改 frontend/src/game/world/{index.js,constants.js,selftest.mjs}。
按环内 斥候→主力→搜剿 执行。必须在本窗终端跑 manifest 的 verify_cmd 并贴输出原文（UTF-8 无 BOM 的 .cmd + cmd /c）。
tests[] 只放可执行命令。先自证伪。不要打开网页预览、不要自标 done。完工说「M5 已完成，请主导窗口查收」。
```

- **要做**：普通树只画 `树木/浅树.png` 第 1 帧，删掉取帧路径与 `浅树-2/-3/-4.png` 预加载；掉落/果实/砍倒/刷树/阻挡全部不变。

---

## 【M2】荆棘 4 帧爆发特效 + 小金刚（脉冲 + 兄弟「再获得一次」战斗侧）

**贴给窗口的话**
```text
我是 M2 窗口。请读 docs/派工-总览.md 与 docs/派工-批次7.md 的【M2】段，以及 .task/briefs/TASK-048-worker.txt。
任务 TASK-048：只改 frontend/src/game/{combat/index.js,combat/selftest.mjs,weapons/index.js}。
按环内 斥候→主力→搜剿 执行。必须在本窗终端跑 manifest 的 verify_cmd 并贴输出原文（UTF-8 无 BOM 的 .cmd + cmd /c）。
tests[] 只放可执行命令。先自证伪。不要打开网页预览、不要自标 done。完工说「M2 已完成，请主导窗口查收」。
```

- **要做**：① **荆棘特效**：触发时以**角色为中心**播 `fx/thorn-burst-1..4.png`（4 帧 / 10fps / 播完消失 / 显示范围跟荆棘半径 / 缺图不崩），不改伤害判定；② **脉冲 3 身位、伤害 ×2**；③ **兄弟「再获得一次」**（大娃 / 四娃 / 五娃 / 二娃按层数 +1，**删掉旧 +10% 分支**）。三娃/六娃归 M3、七娃黑洞归 M5，别碰。

---

## 【M3】兄弟「再获得一次」玩家侧 + 档 8 复活

**贴给窗口的话**
```text
我是 M3 窗口。请读 docs/派工-总览.md 与 docs/派工-批次7.md 的【M3】段，以及 .task/briefs/TASK-049-worker.txt。
任务 TASK-049：只改 frontend/src/game/player/{index.js,selftest.mjs}。
按环内 斥候→主力→搜剿 执行。必须在本窗终端跑 manifest 的 verify_cmd 并贴输出原文（UTF-8 无 BOM 的 .cmd + cmd /c）。
tests[] 只放可执行命令。先自证伪。不要打开网页预览、不要自标 done。完工说「M3 已完成，请主导窗口查收」。
```

- **要做**：① 三娃按层数 +1 生效（选中 +2 层甲、此后每次 +2 层）、六娃失锁时长按层数 +1；**删掉旧 +10% 分支**；② **档 8 复活接口**：`tryRevive()`（就绪 → hp=1、不死、消耗、进入冷却）、冷却＝再发生 **5 次真实回血**（满血不回血不计）、只读 `isReviveReady()` / 剩余次数；**不在 player 里读羁绊档位**（M1 接线）。

---

## 【M6】贪婪入池 + 小金刚 / 生生不息羁绊文本

**贴给窗口的话**
```text
我是 M6 窗口。请读 docs/派工-总览.md 与 docs/派工-批次7.md 的【M6】段，以及 .task/briefs/TASK-050-worker.txt。
任务 TASK-050：只改 frontend/src/ui/{constants.js,session.js,selftest.mjs}。
按环内 斥候→主力→搜剿 执行。必须在本窗终端跑 manifest 的 verify_cmd 并贴输出原文（UTF-8 无 BOM 的 .cmd + cmd /c）。
tests[] 只放可执行命令。先自证伪。不要打开网页预览、不要自标 done。完工说「M6 已完成，请主导窗口查收」。
```

- **要做**：① 贪婪（高级，`greed`）+ 常量与 `greedChance` + 会话侧 `rollGreedBonus()`（0.1s 冷却、真实时间推进）+ **不入任何羁绊** + 图标空白占位；② 小金刚档位文本（脉冲 3 身位 / ×2、兄弟「再获得一次」）与生生不息 **3/5/8**（档 8 复活文本 + 注明当前不可达）。

---

## 【M7】独立验收方（TASK-046…050）

**贴给窗口的话**
```text
我是 M7 窗口。本轮担任 TASK-046/047/048/049/050 的「独立验收方」（reviewer 必须 ≠ 施工窗），这不是我的长期身份。
我这一环同样按 斥候 → 主力 → 搜剿 走。请读 docs/派工-总览.md 与 docs/派工-批次7.md 的【M7】段，以及 .task/briefs/TASK-051-worker.txt。
不要改任何业务代码，不要打开网页预览。
```

- **要做**：五份 `verify-report.json`（`reviewer="M7"`、`worker_window` 分别 M4/M5/M2/M3/M6）+ 逐字复跑 + 自写探针（蝎子弹与冰人弹**真的分开**、树木**真的只画第 1 帧**、荆棘特效 4 帧/居中/随半径且不改伤害、脉冲 3 身位/×2 边界、「再获得一次」逐兄弟逐值、复活（1 血 / 回血 5 次 / 满血不计）、贪婪概率与冷却与不入羁绊）。
- ⚠️ **`changed_files` 必须申报自己 `allowed_paths` 内的全部新增文件**（verify-report + evidence），否则门禁判「越界未申报」（上一轮踩过）。

---

## M1 自己的活（不在任务书里，收口时做）

1. **素材入库**：`thorn.png` / `nourish.png` / `thorn-burst-1..4.png` 已核规格（48×48 不透明底＝与既有图标一致；96×96 透明底）并 `git add`。
2. **`match.js` 接线**：① 贪婪——`onCrystal` 里调 `session.rollGreedBonus()`，命中就 `notifyExp(2)`（等价「再获得一次经验」）；② 复活——`onHurt` 里「生生不息档位 ≥ 8 且 hp ≤ 0」时调 `player.tryRevive()`。
3. 查收、本窗全量重跑、文档回写（设计表 / SPEC / 更新日志 v0.14）、发布、开游戏。
