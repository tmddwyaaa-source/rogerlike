# 交接计划 — 类幸存者（v0.9 → 后续更新）

> **写于**：2026-08-24 · 由主导窗口 M1 生成  
> **交接对象**：第三方开发小组  
> **载体**：本 GitHub 仓库（main = 源码，gh-pages = 在线可玩版）  
> **在线地址**：https://tmddwyaaa-source.github.io/rogerlike/

---

## 一、项目现状快照

- **版本**：v0.9 已发布（main 分支 + gh-pages 静态站，内容一致）。
- **技术栈**：Vue 3 + Vite + Canvas 2D（`frontend/`）；Spring Boot 3 / Java 17 可选统计后端（`backend/`，不开也能玩）。
- **功能全景**：三角色（游侠/战士/法师）蓄力射击；怪物体（蘑菇/蜗牛/裂怪/史莱姆x-1/x-3/蝎子/兰花/灰树/普通树/Boss 冰人）；跟班系统（地精/兔子/蝙蝠/奇怪的蛋）；羁绊（天行健/万物一心/小金刚占位）；20 个升级选项（普通+高级池）；9 种音效 + 总音量×背景音乐×音效三滑条；像素框架 UI（菜单/局内通栏钳制黑边）；回忆系统；难度一/二；测试模式（无敌/满蓄/自选升级/时间滑条等）。
- **模块状态**：M3~M11 全部 `done`（详见 `docs/MODULE-REGISTRY.md`），无 blocked 项。

## 二、文档地图（接手先读）

| 文档 | 内容 |
|------|------|
| `docs/GAME-SPEC.md` | 玩法规格真相源（§4 升级池、§4.3 羁绊、§5~6 怪物、§7 UI） |
| `游戏当前设计表.md` | 面向「玩家可感知内容」的当前状态（根目录） |
| `怪物属性表.md` | 全怪物数值（根目录） |
| `docs/MODULE-REGISTRY.md` | 模块注册表：谁负责什么路径、状态机、每环交付物 |
| `docs/WINDOW-ASSIGNMENTS.md` | 开窗花名册 + 每个 Phase 的开工粘贴块 |
| `docs/RECEIPT-LOG.md` | 全部查收记录（含每次的证据与根因调查） |
| `docs/HANDOFF-P1~P24.md` | 每环任务书（历史，也是格式的模板） |
| `更新日志.md` | 面向玩家的版本日志（根目录） |

## 三、多窗口协作协议（后续更新必须延续）

本项目用多窗口分工开发，**规则写在技能 `multi-window_M` 与各 HANDOFF 里**，要点：

1. **M1（主导窗口）**：写规格与 HANDOFF 任务书、派工、查收、`match.js`/`App.vue` 集成接线、开游戏联调；**只有 M1 能把模块标 `done`、只有 M1 能打开游戏**。
2. **M2~Mn（子窗口）**：只改自己规定路径，环内走 **斥候（只读调查）→ 主力（最小实现）→ 搜剿（自验）**，完工说「M? 已完成，请主导窗口查收」。
3. **卡点上限**：同一问题最多打回 4 次，第 4 次仍失败必须硬停，写 `docs/BLOCKERS/` 升级报告换方案，禁止盲目第 5 次尝试。
4. **查收铁律**：磁盘核对规定路径（用文件 mtime 判定本环改动边界）→ 跑 selftest → `npm run build` → 实机验证，**禁止凭口头「完成了」放行**。
5. **派工前先跑基线**（`node src/ui/selftest.mjs` 等 + build），避免把在途改动误判为回归。

## 四、P25 设计定稿（待实施，已与产品负责人逐项确认）

### 4.1 毒刺怪（300 秒肉盾，接替蘑菇怪刷怪位）

| 项 | 值 |
|----|-----|
| 出场 | 300 秒起蘑菇怪**停刷**（场上存量保留至死亡），刷怪位由毒刺怪接管；每 5 秒一波 `1 + floor(秒/180)` 只 |
| 血量 | `260 + 20×floor((秒−300)/45)`；难度二每阶再 **+5**（走 `getHpGrowthAdd` 既有接口） |
| 移速 | `0.70 + 0.02×阶`，**无帽**（不套 1.4） |
| 击退抗性 | **1 身位**（`knockbackResist = BODY`，同蜗牛：每次击退距离 −1 身位，最低 0） |
| 掉落 | **4 结晶，每个 20% 独立概率**为高级结晶 |
| 贴图 | 「毒刺怪」在小怪素材文件夹（**接手后先核实并两边拷贝**，见 §8 资产规矩） |

### 4.2 高级结晶（新机制）

- 外观：现有结晶贴图**程序绘制紫色外覆层**（零素材成本）。
- 价值：**+5 经验**（普通 1 → 高级 6）。
- 概率**逐个独立掷**（不是一个变全变）。
- 来源仅两处：毒刺怪 20%/个、冰人 10%/个；磁铁/黑洞照常吸取。

### 4.3 冰人调整

| 项 | 旧 | 新 |
|----|-----|-----|
| 击退抗性 | 50%（`knockbackScale=0.5`） | **70%（scale 0.3）** |
| 血量 | 3300 | **3800**（重生 `round(3800×1.4ⁿ)`） |
| 掉落 | 300 结晶 | **150 结晶，每个 10% 独立概率高级** |

### 4.4 七兄弟升级（葫芦娃，均可叠）

| 兄弟 | 池别 | 效果 |
|------|------|------|
| 大娃 | 普通 | 弹体 +40%/次（**已有**，加入羁绊计数） |
| 二娃·千里眼 | **高级** | 三选一 **20%/层** 概率变四选一 |
| 三娃·铜头铁臂 | 普通 | 获得 1 层护甲，**抵挡一次完整伤害**；无限叠；HUD 显示层数 |
| 四娃·喷火 | 高级（见 §5 待定②） | 命中点燃 3 秒，**每秒 10% 攻击**（+10%/层） |
| 五娃·吐水 | 高级（见 §5 待定②） | 命中**减速 20%、0.3 秒**（+0.2/层，单位见 §5 待定①） |
| 六娃·隐身 | 普通 | 每 10 秒失锁脉冲：1.5 身位内敌人 **2.5 秒不锁定角色**（近战不靠近、远程不开火，含蝎子/冰人），每层 +0.5 秒 |
| 七娃 | 普通 | **黑洞仅改名**（id `blackhole` 与效果不动，吸全图结晶） |

### 4.5 羁绊「小金刚」（原占位羁绊演进）

- 名称：**小金刚**；计入 = 上述七兄弟；**档位只有一个：7**（集齐全部七种）。
- 集齐奖励 = **七色脉冲**（唯一奖励，无中间档、无兄弟效果倍率）：
  - 每 **3.0 秒**自动触发，以角色为中心 **1.5 身位**圈；
  - 伤害 **攻击 × 1.3**（每次脉冲单独掷暴击）；
  - 减速 **30%、0.4 秒**；
  - **不**施加击退、**不**触发四娃点燃、**不**打破六娃失锁；
- chip 平时显示进度（如「小金刚 3/7」），集齐后变身形态显示。
- 另新增独立高级升级「**击退**」：每层 +0.5 身位，可叠。

### 4.6 怪物体验改造

1. **生成预警**：怪物出生前落点显示**红圈约 0.8 秒**再出现（红圈素材由原负责人手绘，接手时若未到位先用程序画红色空心圈占位）。
2. **环绕生成**：刷怪位置尽量环绕角色（视野边缘外 1~3 身位环带，可调）。
3. **蝎子独立 CD**：每只蝎子**首次开火随机 0~5 秒错峰**，之后各自 5 秒节奏（射击间隔 10s→**5s**），消灭齐射。
4. 冰人弹幕 CD 22s→**17s**。
5. **规则确认**：怪物移速维持**纯时间制**，玩家升级不影响怪物（仅冰人保留出生×0.7 快照、逃跑=玩家当前+0.2）。

## 五、两处待定项（负责人未来得及拍板，含推荐默认）

1. **五娃「+0.2/层」的单位**：推荐 = 减速**时长 +0.2 秒/层**（0.3→0.5→0.7s…）；备选 = 减速幅度 +20 个百分点/层（20→40→60%，需设帽）。
2. **四娃/五娃是否都进高级池**：负责人原话「四娃·喷火（…）/ 五娃·吐水（…）属于高级选项」倾向于**都进高级**。注意后果：高级池每第 5 次三选一仅 30% 出 1 格，七兄弟中 3~4 种在高级池会显著拉长集齐周期——若测试发现太难，建议把四娃放回普通池。

## 六、P25 工程拆分与验收

按多窗口协议拆分（编号沿用现有模块）：

| 窗口 | 内容 | 规定路径 |
|------|------|----------|
| M4 | 三娃护甲状态、六娃失锁脉冲的角色侧状态 | `game/player/**`、`game/render/**` |
| M5 | 四娃点燃、五娃减速、七色脉冲、高级「击退」 | `game/combat/**`、`game/weapons/**` |
| M6 | 毒刺怪、红圈预警、环绕生成、蝎子错峰 CD、冰人调整 | `game/enemies/**`、`game/spawner/**` |
| M7 | 高级结晶（紫边、+5 经验、独立概率接口） | `game/world/**`、`game/pickups/**` |
| M8 | 七兄弟入池文案、四选一、黑洞改名七娃、小金刚进度 chip、HUD 护甲显示 | `ui/**`、`views/**` |
| M1 | `match.js` 接线（合体触发、脉冲、失锁/护甲钩子）、查收、发布 | `match.js`、`docs/**` |

**验收命令**：各模块 `node src/…/selftest.mjs` 全绿 → `npm run build` → M1 开游戏实机（5173，复用已占用实例）→ 回写 `游戏当前设计表.md` / `GAME-SPEC.md` / `怪物属性表.md` → 更新 `docs/RECEIPT-LOG.md` 与 `MODULE-REGISTRY.md`。

## 七、发布流程（GitHub）

1. 代码：`git add -A && git commit && git push origin main`。
2. 静态站：`cd frontend && GITHUB_PAGES=true npm run build` → 用 worktree 把 `dist/*` 全量替换到 `gh-pages` 分支并推送：
   ```bash
   git fetch origin
   git worktree add __deploy -b gh-pages origin/gh-pages
   # 清空 __deploy 后 cp frontend/dist/* → commit → push origin gh-pages
   git worktree remove __deploy
   ```
3. Pages 为 legacy 模式（gh-pages 分支根目录），约 1 分钟后生效，`curl` 首页 200 + 核对新 hash 资源名即部署成功。

## 八、踩坑实录（都是真实代价换来的，请勿重蹈）

1. **`shell.notifyExp(1)` 返回的是「本次升了几级」，不是「获得经验」**——普通结晶吸收返回 0。曾因此让拾取音效只在触发升级的结晶上响（追查三轮才定位）。
2. **一次性 `Audio` 播完即弃会被 GC 静默掐断**——必须持有引用到 `ended/error`（见 `ui/sfx.js` 的 `playing` Set）。
3. **素材电平要实测**：拾取音效曾因录音 RMS −28dB（其他 −14~−19）被当成「没声音」——用 Web Audio `decodeAudioData` 算 RMS 排查，代码用增益表补偿（`SFX_GAIN`）。
4. **`pointer-events: none` 会杀死 CSS `:hover`**——羁绊 tooltip 写好了两周不可见，根因是容器 none 未在 chip 上恢复 auto。
5. **资产两边规矩**：运行时素材必须同时放 `frontend/public/assets/` 与 `assets/source/`（哈希一致）；升级图标走 M10 过审流程，未过审空白方块占位。
6. **边界核查用 mtime**：子窗口交付后按「本环修改时间」核对是否越界改了别人的路径。
7. **画布钳制**：`.rl-root` 上有 `--rl-stage-left/top/width/height`（App.vue 维护），任何局内 UI 消费时必须带 fallback，否则 Node selftest 全崩。
8. **升级文案断言**：`ui/selftest.mjs` 用字符串断言锁了大量类名/文案（甚至 `282px`），改 UI 时同步改断言；「敬请期待」禁串只限 StartView。

## 九、进行中 / 待办

- **M10 图标**：二娃~六娃、击退、生成红圈的图标由原负责人手绘中（升级图标规格见 `docs/ICON-STYLE.md`，12×12 小像素；未到位先空白方块占位）。
- **毒刺怪贴图**：负责人说在小怪文件夹——接手后第一件事核实 `assets/source/小怪/` 与 `frontend/public/assets/小怪/` 两边是否齐。
- 待定项见 §5；定案后按 §6 派工。

---

*本文件由 M1 于交接日生成。项目使用多窗口协议从 P1 推进至 P24，全部过程证据在 `docs/RECEIPT-LOG.md`。祝顺利。*
