# 类幸存者 — 项目经历与技术栈

> 个人网页游戏项目（仓库名 `rogerlike`）。本文面向简历与 GitHub 介绍，对齐 **2026-08-18** 暂停点（P11 可玩版本）。  
> 玩法数值以根目录 `游戏当前设计表.md` 与 `docs/GAME-SPEC.md` 为准。

---

## 一句话

用 **Vue 3 + Vite + Canvas 2D** 从零做一款网页端「类吸血鬼幸存者」肉鸽小游戏：固定大地图、镜头跟随、蓄力弓、刷怪成长、升级三选一、跟班，以及可选的 **Spring Boot 对局统计**。

在线试玩：[https://tmddwyaaa-source.github.io/rogerlike/](https://tmddwyaaa-source.github.io/rogerlike/)

---

## 简历可摘录（3～5 条）

- 独立完成网页端 2D 俯视像素肉鸽：**Canvas 主循环**、整数倍像素缩放、相机跟随、`11000×11000` 世界与 `400×225` 逻辑视野。
- 拆分为玩家 / 战斗 / 敌人刷怪 / 环境掉落 / 跟班 / UI 会话等模块，用 **Node 自测脚本 + Vite 构建** 做回归，改数值先改常量再回写设计表。
- 实现肉鸡循环：经验结晶 → 无上限升级 → 三选一（含高级项概率）→ 暂停冻结；跟班独立索敌与碰撞伤害。
- 前端可完全离线游玩（设置与「回忆」走 `localStorage`）；后端 **Spring Boot 3 + MyBatis-Plus + MySQL** 仅做开局/结算上报，接口失败不影响开局。

---

## 技术栈

### 前端（游戏本体）

| 层 | 选型 | 版本 / 说明 |
|----|------|-------------|
| 框架 | Vue 3（Composition API + SFC） | `3.5.38` |
| 构建 | Vite | `8.1.0`；插件 `@vitejs/plugin-vue 6.0.8` |
| 渲染 | Canvas 2D | 整数倍缩放、关闭 alpha、相机跟随 |
| 语言 | JavaScript（ES Module） | 无 TypeScript；模块自测为 `.mjs` |
| 状态 / 流程 | 自研 `session` | `menu → char → difficulty → playing / levelup / upgrade → result` |
| 持久化 | `localStorage` | 音量、测试开关、最近 10 局回忆 |
| 音频 | Web Audio / `<audio>` 封装 | 循环 BGM，音量 0 即静音 |

### 后端（可选统计，不阻塞游玩）

| 层 | 选型 | 版本 / 说明 |
|----|------|-------------|
| 运行时 | Java 17 | Maven |
| 框架 | Spring Boot | `3.0.4` |
| Web | `spring-boot-starter-web` | REST `/api` |
| ORM | MyBatis-Plus | `3.5.3.1` |
| 数据库 | MySQL | 对局表：存活秒、击杀、等级、经验、难度 |
| 其它 | Lombok、Hutool | DTO / 工具 |

接口概要：`GET /api/health`，`POST /api/matches/start`，`POST /api/matches/{id}/end`，`POST /api/matches`，`GET /api/matches/top`。

### 工程与验证

- 前端目录：`frontend/`（`npm run dev` / `build` / `preview`）。
- 后端目录：`backend/`（`mvn`）。
- 模块自测：`player` / `combat` / `enemies` / `world` / `companions` / `ui` / `match` 各自 `selftest.mjs`。
- 规格文档：`docs/GAME-SPEC.md`、设计表、怪物属性表、模块注册表、查收记录。

---

## 仓库结构（逻辑）

```
rogerlike/
├── frontend/                 # 可玩客户端
│   ├── src/game/             # 引擎、对局接线、玩家、弓、敌人、世界、跟班
│   ├── src/ui/               # 升级池、会话、回忆、设置、统计 API
│   ├── src/views/            # 主页 / HUD / 三选一 / 设置 / 结算
│   └── public/assets/        # 运行时贴图与 BGM
├── backend/                  # 对局统计 API（可选）
├── assets/source/            # 素材仓库备份（与 public 目录结构对齐）
├── docs/                     # 规格、素材、模块状态、查收
├── 游戏当前设计表.md
├── 怪物属性表.md
└── 项目经历.md               # 本文件
```

玩法循环在前端闭环。后端挂了：开局 `catch` 后 `matchId = null`，局内照常，结算上报失败也不打断流程。

---

## 做了什么（经历按系统）

### 1. 引擎与镜头

- 固定世界、镜头跟角色；视野 `400×225`，按窗口做 **整数倍** 像素放大，避免半像素发糊。
- `match.js` 把玩家、战斗、敌人、环境、跟班接到同一帧：`playing` 才推进世界；升级 / 暂停冻结战斗，仍可播 `+1`、结晶可飞。

### 2. 角色与武器

- 游侠：WASD、3 心、受击无敌与像素飞散；贴图分向 Idle/Walk/Attack/Hurt/Death。
- 游侠弓：按住蓄力、松开射击；未蓄伤 = 攻击，满蓄 ×2；击退 1～2 身位（可减免，最低 0）；满蓄基础穿透。
- 独立攻击属性（初值 20）；升级改的是属性，再映射到弹伤害。

### 3. 敌人、环境、掉落

- 刷怪环：视野外、距角色 ≤ 1000。
- 蘑菇怪 / 蜗牛怪 / 裂怪 / 史莱姆 x-1、x-3；灰树可打、自损、毁后延迟爆裂怪。
- 普通树仅子弹可毁；结晶吸附、磁铁、黑洞吸入飞行。
- 难度随存活时间成长（每波数量、生命、移速）。当前停在「难度一」、10 分钟强制胜利。

### 4. 成长与 UI

- 结晶经验、无等级上限；先播 `+1` 再出三选一。
- 普通升级池 + 每第 5/10/15… 次 30% 插入高级项（地精）。
- 每升 5 级（6 / 11 / 16…）角色攻击 +1、移速 +0.05。
- 设置（音量、返回确认、测试模式）；「回忆」最近 10 局，图标悬停显示效果。

### 5. 跟班

- 地精（高级）：碰撞伤害、全跟班伤害加成。
- 兔子（普通）：独立锁敌、从怪朝角色一侧切入重叠再打。
- 多跟班尽量领不同目标，走进绘制矩形才结算，避免叠成一张图空追。

### 6. 怎么推进项目

不是一次性堆功能，而是 **规格 → 分模块实现 → 自测 → 查收 → 回写设计表**。  
卡点有上限、有打回，避免同一 bug 盲改。适合写进简历的是：**可验证的模块边界**，不是「窗口编号」。

---

## 当前完成度（暂停点）

**已经可玩：** 完整一局（开局 → 生存/失败或 10 分钟胜利 → 结算）；升级与跟班；测试模式便于调参。

**刻意未做完 / 已知限制：**

- 仅一角一武器、仅难度一。
- 统计 API 绑本地 MySQL；静态托管时默认不上报（见下文讨论）。
- 部分升级图标来自手绘/过审 PNG；个别仍可能是占位风格。
- 素材含 Craftpix 角色包与自制/收集 PNG，公开仓库前需核对授权。

---

## 本地运行（给以后的自己 / 克隆者）

```bash
cd frontend
npm install
npm run dev
```

浏览器打开 `http://localhost:5173/`。  
统计后端可选：本机 MySQL 建库后启动 `backend`（默认 `8080`）。不启动也能玩。
