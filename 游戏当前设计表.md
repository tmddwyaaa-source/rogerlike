# 类幸存者 — 游戏当前设计表

> **对齐基准**：以当前代码常量为准；与旧文档冲突时以代码为准。  
> **主要来源**：`frontend/src/game/**`、`frontend/src/ui/**`、`frontend/src/game/match.js`；规格 `docs/GAME-SPEC.md`。  
> **对齐日期**：2026-09-11（**P42 批次 1 落地**：技巧每层蓄力 **−0.15**（基础 0.75s → **5 个**到 0）；唯快不破由高级降为**普通**（仍受「技巧已退池」门控）；强化射击改为**游侠专属**，高级项 10 → **9**；高级出现机制改为代码口径「等级为 5 的倍数时第 1 格必出高级项、其余格各自 30%，非 5 倍数等级不出高级」）。同日 **恶魔跟班口径定稿**：只在离角色 **3 身位内**取最近的怪（3.5 身位释放），不追远处的 Boss；软拉绳绳长保持 6 身位；万物一心**档 8** 的「优先角色目标」对恶魔无效；兔子基础伤 **10**；跟班恢复「不抢已被占用目标」的预登记。此前 2026-09-10 全表与代码对齐：升级池补齐 驯兽师 / 恶魔 / 史莱姆gg / 伴我同行 四项；四娃点燃每秒 **30%**（集齐 40%）、三娃改为「选中 +1 甲、此后每升 N 级再 +1 层」、击退改每层 **+1 身位**；万物一心计入项补齐 **8** 种、档 8 可达；§10 只剩「击退 / 伴我同行」图标空白）。此前 P35（**局外 UI 重构为阳光草地同世界观**：六屏回到浅草地 `--rl-screen #c5e0a3`，文字/描边用现有墨色 `--rl-ink`，强调色只用现有面板绿/按钮黄/纸白；悬停只给箭头/1px 描边/下压，不改填充；Zpix 字体作用域收窄到局外，局内 HUD 字体恢复原等宽栈；**局内 HUD、玩法、文案、事件、ESC 全未变**。P34 的夜蓝/墨绿石碑方向已废弃，见 §2 视觉段）；此前 P25 定案：七兄弟（二娃~六娃/击退新增、黑洞改名七娃）、小金刚羁绊（集齐7种=七色脉冲+各兄弟效果+10%）、毒刺怪、高级结晶、冰人调整、蝎子错峰 CD/预警/环绕生成

---

## 1. 产品概览

| 项 | 当前实现 |
|----|----------|
| 游戏名 | **类幸存者** |
| 类型 | 网页端、类吸血鬼幸存者、2D 俯视像素 |
| 叙事 | 暂无剧情背景 |
| 视觉 | 浅绿简约；草地色板对齐浅树 |
| 技术 | Vue 3 + Vite + Canvas；Spring Boot 3 统计局数据 |
| 可玩角色 | **游侠** / **战士** / **法师** |
| 可玩武器 | 随角色：游侠弓 / 近战挥砍 / 法球（无选武器页） |
| 难度 | **难度一**（`id: "1"`）/ **难度二**（`id: "2"`） |
| 通关 | 难度一：存活 **600 秒**。难度二：击败 Boss **2** 次 **且** 存活 600 秒（满 10 分钟不够杀则继续） |
| 失败 | 生命归零 → **先播完死亡动画约 1 秒** → 再结算 |

---

## 2. 操作与 UI 流程

> **视觉（P23 起全画面统一；P34 局外换装）**：像素框架——`.rl-frame` 浅色（菜单）/ `.rl-frame--dark` 深色（局内）；零圆角、无渐变、过渡仅 `steps()`；色板/间距/字号统一走 `pixel.css` `:root` 变量。**菜单流与局内 HUD 顶/底通栏均钳制在画布黑色留边内侧**（消费 `--rl-stage-*`）；**齿轮（窗口右上 10px）与「……」（窗口左下 10px）以窗口为参照**；升级卡 160px 深色卡。
> **局外视觉（P35，仅首页/选角/难度/设置/回忆/结算）**：与局内同属阳光草地世界——页底 `--rl-screen #c5e0a3`（与局内草地同色）；文字与细边框用现有 `--rl-ink #2c2c28`；强调色仅取现有 `--rl-panel` / `--rl-btn` / `--rl-paper`；轻量像素框 `.rl-panel`（1px 墨边 + 浅底，无厚重双层）；可选行 `.rl-opt`（悬停/选中只出左侧 1px 像素箭头 + 1px 描边加强 + 下压 1px，填充色不变）；标题为草地上的 Zpix 大字 + 深色 1px 描边，无石碑标题框；首页无底部横条。字体：Zpix 只作用局外（`.rl-screen` 及局外弹层），局内 HUD 保持原等宽栈。**局内 HUD（心/计时/羁绊/经验条/齿轮/「……」）样式、布局与字体零改动。**
> **音效（P20 起；P23 增益与三滑条）**：`/assets/游戏音乐/`——开火（`射箭声音.wav` 按角色 / 挥砍 / 法师火球）、拾取结晶（**每颗吸收即播一声，可叠加，不节流**；增益 ×5，素材电平低）、角色受伤、升级 +1（**增益 ×2.8**）、剩 1 心心跳循环（回血/死亡停）、游戏失败、通关；音量走**总音量 × 分项**三滑条。右侧羁绊 chip **单排一列**，悬停显示效果与全部档位（已达深色/未达浅色）。  

### 2.1 阶段

| phase | 含义 |
|-------|------|
| `menu` / `char` / `difficulty` | 主页 → 选角 → 选难度 |
| `settings` | 设置（菜单进入，或局内暂停） |
| `playing` / `upgrade` | 局内 / 升级三选一 |
| `result` / `victory` | 死亡结算 / 通关 |

### 2.2 操作

| 输入 | 效果 |
|------|------|
| WASD | 移动 |
| 鼠标 | 瞄准；**按住蓄力、松开发射** |
| 齿轮（窗口右上）或 ESC | 局内暂停并打开设置 |
| ……（场景外左下） | 打开更多；其中「回忆」查看最近 10 局 |
| ESC（设置中） | 关闭设置并恢复 |

### 2.3 设置

| 项 | 默认 | 说明 |
|----|------|------|
| 总音量 | 0.7 | 滑条实时百分比；持久化 localStorage；实际 BGM = 总音量 × 背景音乐；实际音效 = 总音量 × 音效音量 |
| 背景音乐 | 0.7 | P23 新增；只管 BGM；滑条实时；持久化 |
| 音效音量 | 0.7 | 只管全部一次性音效与心跳；滑条实时；持久化 |
| 返回主页 | — | 局内需确认：「返回后本局无法保持，是否退出？」 |
| 测试模式 | 关 | 绿=打开 / 暗色=关闭（P19 对调） |
| └ 无敌 | 关 | 血量最低 1 |
| └ 满蓄模式 | 关 | **蓄力上限视为 0**（无需蓄力，满伤射出） |
| └ 刷怪速度 | 1 | 0.25～2，步进 0.25。**仅测试模式**。与当前时间该种怪的每波数量相乘（四舍五入，至少 1） |
| └ 提高等级 | 0 | 仅测试模式；**0～3**（提高当前等级，不是跳到某级）。**关掉设置后**先播 +1，再弹出三选一，最多 3 次 |
| └ 时间 | 0 | 仅测试模式；左右滑条 **0～10 分钟**，写入局内存活时间 |
| └ 升级选项自选 | — | 仅测试模式；弹出全部升级（按创建顺序），悬停看效果，点击获得；ESC / 右上红 X 关闭 |
| └ 火柴人 | 关 | 仅测试模式；在角色右侧 **3 身位**生成木桩（999 血，每秒回 500，被打才动一下） |

### 2.4 升级三选一（从池中随机抽 3）

图标：已过审 PNG 用图；当前只有 **击退 / 伴我同行** 没有过审图，局内用空白方块占位。升 1 级时先出现角色旁 **+1**，播完再出三选一。  
普通池日常抽 3 项。**当次升级到达的等级是 5 的倍数（Lv.5 / 10 / 15…）时**：三选一**第 1 格必出高级项**，其余格各自独立 **30%** 概率换成高级项；**非 5 倍数的等级不出高级项**。高级共 **9** 项：二娃、四娃、五娃、击退、地精、恶魔、强化射击（**游侠专属**）、精益求精、奇怪的蛋。（唯快不破已降为**普通**项，但仍要等技巧从池中移除后才出现。）  
升级不再白送攻击 / 移速 / 空心 / 穿透。这些改由羁绊 **天行健** 按档位与角色等级发放。

| ID | 名称 | 效果 |
|----|------|------|
| `move_speed` | **敏捷** | 移速 +0.15（设计单位） |
| `ammo_cap` | **散射** | 弹道 +1，伤害 −3（可叠） |
| `reload` | **技巧** | 蓄力时间 **−0.15s**（基础蓄力 **0.75s** → **5 个**到 0）；减到 ≤0 后从池中移除 |
| `power` | **力量** | 攻击 +10 |
| `survive` | **生存** | 血上限 +1，当前血 +1（2 滴 → 3/4，不会加满） |
| `recover` | **恢复** | 回复 2 滴血（不超过上限） |
| `earth` | **大地啊** | 普通树每波 +2，果实概率 +10% |
| `pierce` | **穿透** | 穿透 +1（未蓄/满蓄都加；可叠）。法师每 1 点穿透伤害 **+20%** |
| `eyes` | **开眼了** | 背后弹道 +1，伤害 −3（叠法同散射） |
| `giant` | **大娃** | 每次弹体大小 +40%（相对基础：1 次 ×1.4，2 次 ×1.8） |
| `erse` | **二娃·千里眼** | **高级**。三选一 **20%/层** 概率变四选一（集齐小金刚 30%/层）。图标 `upgrades/erse.png` |
| `sanwa` | **三娃·铜头铁臂** | **普通**。选中立即 **+1 层护甲**；之后每累计升 **N** 级再 +1 层，`N = 11 − 已选次数`（第 1 次 N=10、第 2 次 N=9…，可叠）。有甲时该次伤害 ×0.7 并扣 1 层；HUD 显示层数。图标 `upgrades/sanwa.png` |
| `siwa` | **四娃·喷火** | **高级**。命中点燃 3 秒，**每层**每秒 **30%** 攻击（集齐小金刚每层 40%）。图标 `upgrades/siwa.png` |
| `wuwa` | **五娃·吐水** | **高级**。命中减速 20%（集齐小金刚 30%）、0.3 秒（+0.2 秒/层）。图标 `upgrades/wuwa.png` |
| `liuwa` | **六娃·隐身** | **普通**。每 10 秒失锁脉冲：1.5 身位内敌人 1.0 秒不锁定角色（每层 +0.5 秒）。图标 `upgrades/liuwa.png` |
| `knockback` | **击退** | **高级**。每层 **+1 身位**击退。图标空白（待出图） |
| `blackhole` | **七娃** | **黑洞仅改名**（id `blackhole` 与效果不动，吸全图结晶） |
| `magnet` | **磁铁** | 结晶吸取范围 +1 身位（可叠） |
| `goblin` | **地精** | **高级 / 跟班**。生成 1 个地精，**所有跟班伤害 +10**（可叠）。碰撞重叠才打；0.4s；伤 ceil(15+攻击×0.6)+加成；两敌彼此重叠才打 2 个。图标左上 **红点** |
| `empower_shot` | **强化射击** | **高级 / 游侠专属**（战士、法师池中不出现）。游侠第 1 次：满蓄改为激光，ceil(攻击×2.5)，穿透 +2，过量溢出，**攻击 +5**；再次 +15。图标左上红点。卡片文案统一为游侠版 |
| `rabbit` | **兔子** | **普通 / 跟班**。生成 1 个兔子。碰撞才打；0.4s；伤 **10**+跟班加成；最多 1 个敌人。独立索敌，从怪朝角色一侧切入。图标 `upgrades/rabbit.png` |
| `bat` | **蝙蝠** | **普通 / 跟班**。生成 1 个蝙蝠。伤 **7**+跟班加成；该蝙蝠每杀 200 敌人，角色回 1 心。图标 `upgrades/bat.png` |
| `crit` | **暴击** | 暴击率 **+10**（可叠；判定最高 100%）。图标 `upgrades/crit.png` |
| `only_fast` | **唯快不破** | **普通**。攻击速度 +20%（不改蓄力）。可叠。仅在**技巧已从池中移除**（本局蓄力上限已到 0）后出现。图标 `upgrades/only_fast.png` |
| `refine` | **精益求精** | **高级**。暴击率每 30% 使暴击伤害 +0.2。可叠。图标 `upgrades/refine.png` |
| `tamer` | **驯兽师** | **普通 / 跟班**。所有跟班伤害 **+5**、跟班移速 **+0.05** 设计单位（可叠）。图标 `upgrades/tamer.png` |
| `demon` | **恶魔** | **高级 / 跟班**。生成 1 个恶魔跟班：伤 `floor(10 + 角色伤害×20%)`+跟班加成，最多 1 个目标。**软拉绳**跟随角色：舒适 2 身位、绳长平衡点 6 身位（软参考，无硬墙）。**索敌：只在离角色 ≤3 身位的活怪里取最近的一只**（已锁目标超出 3.5 身位才放弃），允许与地精/兔子选同一目标；**万物一心档 8 的「优先角色目标」对它无效**。半径内没怪就回到角色身边环绕，不追远处的 Boss。图标 `upgrades/demon.png` |
| `slime_gg` | **史莱姆gg** | **普通 / 跟班**。生成 2 个史莱姆跟班（g-1、g-2），基础伤 **5**。图标 `upgrades/slime_gg.png` |
| `companionship` | **伴我同行** | **普通 / 跟班**。角色每击杀 **100** 怪物，跟班伤害 **+1**（每多 1 层速率 +0.5）。图标空白（待出图） |
| `strange_egg` | **奇怪的蛋** | **高级 / 跟班**。生成 1 个蛋。可叠。三阶段见跟班。图标 `upgrades/strange_egg.png` |

回忆里查看对局：图标从左到右，图标之间留 `×000` 宽；次数为 1 也显示 ×1。鼠标对准图标显示该选项效果。高级选项图标左上 3×3 红点。

### 2.4b 羁绊

选过对应 **种类**（不重复 id，叠同一项不加档）达到档位后生效。没档的不显示。已有档位的画在画布 **右侧** 深色空白，**单排一列**（如「天行健 4」），悬停显示效果与全部档位（已达深色/未达浅色）。

| 羁绊 | 计入选项 | 档位 | 效果（可叠，按角色等级补发） |
|------|----------|------|------------------------------|
| **天行健** | 敏捷、力量、生存、穿透、技巧、唯快不破、暴击、精益求精、散射、开眼了、强化射击 | 2 / 4 / 6 / 8 | 2：每 2 级 +0.01 移速；4：每 2 级 +1 伤害；6：每 10 级 +1 空心；8：每 15 级 +1 穿透。悬停文案（P24）：随等级提高移速/伤害/空血上限/穿透 |
| **万物一心** | 地精、兔子、蝙蝠、奇怪的蛋、驯兽师、恶魔、史莱姆gg、伴我同行 | **2 / 4 / 6 / 8**（P24 改） | 2：跟班伤害 +5；4：跟班 +floor(角色攻击×20%)；6：跟班可打目标 +1；8：跟班移速 +0.2 设计单位、伤害再 +10、**优先攻击角色正在攻击的目标**（**恶魔例外**：恶魔仍取离角色最近的）。池中现有 **8** 种，**档 8 可达** |
| **小金刚**（P25 定案） | 大娃、二娃、三娃、四娃、五娃、六娃、七娃 | 7 | 集齐七种：每 3.0 秒**七色脉冲**（以角色为中心 1.5 身位圈，伤害 攻击×1.3，每次单独掷暴击，减速 30%/0.4s，不击退/不点燃/不破六娃失锁）；且每个兄弟效果值 **+10%（加法）**：大娃 +50%/层、二娃 30%、四娃每秒 40%、五娃 30%、三娃 +1 层甲、六娃 +0.25s、七娃黑洞再触发一次。仅在集齐 7 种后右上角显示「小金刚 7」（对齐前两羁绊；无 n/7 进度），悬停见全档位 |

### 2.5 跟班

独立属性。无血、无击退。移速 = 选择该跟班时角色当前速度。每只跟班独立锁定不同敌人（**恶魔例外**：永远取离角色最近的活怪，允许与其他跟班同一目标），走进重叠才造成伤害。兔子从怪朝角色的一侧切入，不跟在怪身后空追。没敌人则跟角色。多只跟班不要叠成一张图。地精贴图 `跟班/地精.png`，兔子 `跟班/兔子.png`，蛋 `跟班/奇怪的蛋-x/y/z.png`，蝙蝠 `跟班/蝙蝠.png`，恶魔 `跟班/恶魔.png`，史莱姆gg `跟班/史莱姆g-1.png` / `跟班/史莱姆g-2.png`。碰撞重叠才造成伤害。

蝙蝠：伤 7+跟班加成；最多 1 目标；该蝙蝠自己每杀 200 敌人，角色回 1 心。万物一心达标后全体跟班按档吃：+5 / floor(攻击×20%) / 目标 +1 / 移速 +0.2、伤 +10、优先攻击角色目标（P24）。

蛋：角色击杀 100 → 二阶段，再 200（累计 300）→ 三阶段。伤害为角色攻击的 20% / 50% / 80%；目标 1 / 2 / 3（2～3 与地精相同：须彼此重叠）。该蛋自己每杀 100 敌人 +1 固定伤害。吃地精跟班 +10。

恶魔：伤 `floor(10 + 角色伤害×20%)` + 跟班加成；最多 1 目标。**软拉绳**——舒适距离 **2 身位**、绳长平衡点 **6 身位**（软参考，无硬墙）。索敌**只在离角色 ≤3 身位**的活怪里取最近的一只（已锁目标超出 3.5 身位才放弃，避免边界抖动），**允许与其他跟班选同一目标**；万物一心档 8 的「优先角色目标」对恶魔无效。半径内没怪就回到角色身边环绕。贴图 `跟班/恶魔.png`。恶魔还给角色伤害联动加成：每档 5 点跟班伤害 → 角色伤害 +3，多一只恶魔再 +1/只。

史莱姆gg：一次生成 **2** 只（`跟班/史莱姆g-1.png`、`跟班/史莱姆g-2.png`），基础伤 **5**，最多 1 目标。驯兽师：所有跟班伤害 +5、跟班移速 +0.05 设计单位。

---

## 3. 主角

三种角色尺寸相同：宽 **11**、高 **22**；受伤 −1；无敌约 **0.85s**；移速 **1.20** 单位 × 80 = **96** px/s。  
生命：**游侠 3 / 战士 4 / 法师 2**。  
升级不再白送攻/速/空心/穿透（改羁绊天行健）。选角立绘均为侧向 Idle 第 0 帧朝右；**鼠标指向角色**显示「基础属性」+「角色特点」（满蓄百分比）。死亡 8 帧溶解，播完再结算。命中显示本次**实际扣血**（有甲为 ×0.7 后；灰树/浅树也弹；每个单位单独一组；间距略紧；≥100 黄边、≥200 红边；回血绿边）。选难度：难度一悬停「目标：活够10分钟」；难度二「目标：击败Boss 2次，并活够10分钟」。默认暴击率 **0**，暴击伤害 **×1.5**；判定暴击率最高按 100%，超过仍可叠给精益求精。游侠满蓄弹体默认 **×1.25**，再叠大娃；战士未蓄挥砍从身体前起 **1 身位**（沿鼠标长条，厚度约 0.5 身位），蓄力线性到范围/伤害 **×1.6**，大娃加长度与厚度、不把长度当正方形边；法师随蓄力到 **×3.0**，再叠大娃。

| 角色 | charId | 贴图 | 生命 | 攻击 | 穿透 未蓄/满蓄 |
|------|--------|------|------|------|----------------|
| 游侠 | `ranger` | `characters/1/` | 3 | 20 | 0 / 1；强化满蓄 +2，过量可溢出 |
| 战士 | `warrior` | `characters/2/` | 4 | 22 | 无限穿透；基础击退 0；每 1 穿透 +0.5 身位击退；蓄力伤害/范围线性到 ×1.6 |
| 法师 | `mage` | `characters/3/` | 2 | 25 | 弹体碰到即停；每 1 穿透 +20% 伤害；满蓄 +50%；蓄力弹命中后 0.3s 扩散并淡出 |

身位 `BODY=22`。无贴图时火柴人兜底。

---

## 4. 武器

蓄力上限 **0.75s**；按住蓄、松开打；间隔 **0.48s**（唯快不破每次 ÷1.2）。未蓄 = 攻击；游侠满蓄 = 攻击 ×2（强化射击满蓄 ceil(攻击×2.5)）；战士伤害随蓄力线性到 ×1.6；法师 = 攻击 ×（1 + 穿透×20% + 蓄力×50%）。游侠击退 **1 → 2** 身位；战士基础击退 **0**，每 1 穿透 +0.5 身位。最低 0。出手有暴击率，默认 0；触发按 min(率,100%)；暴击时伤害 ×（1.5 + 0.2×floor(率/30)×精益求精层数）。

| 角色 | 弹体 | 说明 |
|------|------|------|
| 游侠 | `Arrow.png`；强化为 `强化箭矢x.png` 激光图集，速度 680 | 沿弹道拉长切帧，不要整张射出 |
| 战士 | `子弹/挥砍-1/2/3.png` | 6 帧；从身体前沿鼠标 **1 身位**长条（厚约 0.5 身位）；蓄力线性到 ×1.6；大娃加长与略加厚；贴图可略大于判定但不超过远端；无限穿透；只结算一次 |
| 法师 | `子弹/法球.png` | 未蓄约 7～8px；满蓄 ×3.0；点射碰到消散；蓄力弹命中后 0.3s 扩散变大并变淡，开始扩散时对最大范围内全体结算，淡到消失 |

---

## 5. 战斗与掉落

### 5.1 经验

| 项 | 值 |
|----|-----|
| 需求 | 开局 Lv.0；`7 + 4×level`（升到下一级；Lv.0→Lv.1 为 7） |
| 上限 | **无** |
| 来源 | **拾取结晶**（1 结晶 = 1 经验） |
| 升级表现 | 每升 1 级，角色周围出现程序绘制的 **+1**（上浮消失）；**播完后**才出现三选一。自然捡结晶与测试提高一样要停顿。升级期间结晶可飞向角色，**不拾取、不加经验** |

### 5.2 掉落

| 来源 | 掉落 |
|------|------|
| 蘑菇怪 / 蜗牛怪 | 1 结晶 |
| 史莱姆x-1 / 蝎子怪 | 2 结晶 |
| 史莱姆x-3 | 0 或 1 结晶 |
| 冰人 | **150** 结晶（每个 **10% 独立概率**为高级结晶） |
| 毒刺怪 | **4** 结晶（每个 **20% 独立概率**为高级结晶） |
| 高级结晶 | **+5 经验**（普通 1 → 高级 6）；现有结晶贴图程序绘制紫色外覆层；逐个独立掷 |
| 普通树 | 结晶 **3～(6+2×满分钟数)**；水果 **30%**（+1 心）。0 分 3～6，1 分 3～8，以此类推 |
| 吸附 | ≤ **2 身位** 自动吸入；磁铁每次 +1 身位 |
| 吸入速度 | **135** px/s |

---

## 6. 怪物与危险物

刷怪环：**视野外** 且距玩家 ≤ **1000**。  
蘑菇怪 / 灰树难度阶：`t = floor(存活秒 / 45)`。  
加新怪物：填根目录 **怪物属性表.md**。  
蘑菇怪移速：`px/s = 80 × min(1.4, base + 0.02t)`，**不乘**玩家当前移速。
击退：`max(0, 原始 − 减免)`。有甲时挨打扣 1 层甲，该次伤害 ×0.7；有甲时素材不透明像素描 1px 黄边（不要方框）。

### 6.1 蘑菇怪

| 项 | 值 |
|----|-----|
| 生命 | **32 + 10t** |
| 绘制 | 32×32 单帧，缩放到约 16～32 |
| 接触 | −1 心；缩小 hurtbox |
| 可击退 | 是 |
| 刷怪 | **每 5 秒** 一波；每波 **5 + floor(秒/120)**（2 分钟 6 只，持续 +1）；**300 秒起停刷**，由毒刺怪接管 |
| 移速 base | **0.62**（t=0 为 49.6 px/s） |
| 素材 | `/assets/小怪/蘑菇怪.png` |

### 6.2 蜗牛怪

| 项 | 值 |
|----|-----|
| 出场 | 存活 **≥120 秒**；成长从 120s 起算 |
| 生命 | **50 + 20×floor((秒−120)/40)** |
| 绘制 | 32×32 单帧 |
| 接触 | −1 心 |
| 击退 | 减免 **1 身位**（最低 0） |
| 刷怪 | **每 5 秒** 一波；每波 **4 + floor((秒−120)/45)** |
| 移速 | **0.62 + 0.01×floor((秒−120)/45)** 设计单位（无 1.4 帽） |
| 甲 | 出生 1 层；可无限叠。挨打扣 1 层，该次伤害 ×0.7；有甲暗黄边 |
| 素材 | `/assets/小怪/蜗牛怪.png` |

### 6.3 灰树

| 项 | 值 |
|----|-----|
| 生命 | `80 × (1 + 0.15t)`（t=0 为 80） |
| 可击退 | **否** |
| 子弹 | 可打 |
| 撞玩家 | −1 心并**立即销毁** |
| 出场 | 存活 **≥30 秒** 后才刷；解锁瞬间先刷 1 波 |
| 每波 | **固定 2 棵** |
| 间隔 | `max(2.0, 5 × 0.94^t)` |
| 自损 | **每棵独立**：每 **3 秒** 随机扣 **1～(8+满分钟数)** |
| 毁坏 | **1 秒后**原位生成 **4** 只裂怪（贴图 `/assets/小怪/裂怪.png`），绕爆点隔开 |
| 裂怪移速 base | **0.77**（t=0 为 61.6 px/s） |
| 素材 | `/assets/树木/灰树.png` |

### 6.4 普通树

| 项 | 值 |
|----|-----|
| 生命 | `50 + 5t` |
| 销毁 | **仅子弹**；碰撞只挡 |
| 开局 | 视野外 **3** 棵 |
| 刷取 | 每波 1；间隔 `max(5, 10×0.97^t)` |
| 掉落 | 结晶 **3～(6+2×满分钟数)**；果 30% |
| 素材 | `/assets/树木/浅树.png` |

### 6.5 史莱姆x-1

| 项 | 值 |
|----|-----|
| 出场 | 存活 **≥180 秒**；成长从 180s 起算 |
| 生命 | **120 + 22×floor((秒−180)/45)** |
| 绘制 | 32×32 单帧 |
| 接触 | −1 心；可击退；减免 0 |
| 刷怪 | **每 15 秒** 一波；每波 **6 + floor((秒−180)/45)** |
| 移速 | **0.87 + 0.01×floor((秒−180)/45)** 设计单位（无 1.4 帽） |
| 掉落 | 2 结晶 |
| 死亡 | 原地立刻 **3** 只史莱姆x-3 |
| 素材 | `/assets/小怪/史莱姆x-1.png` |

### 6.6 史莱姆x-3

| 项 | 值 |
|----|-----|
| 出场 | 不独立刷；x-1 死亡时原地 ×3 |
| 生命 | `ceil(该 x-1 生成时血量 × 0.25)` |
| 移速 | x-1 死亡当时移速 **+0.10**（快照） |
| 接触 | −1 心；可击退；减免 0 |
| 出生 | **0.3 秒**无敌（子弹/跟班打不中，也不伤玩家） |
| 掉落 | 0 或 1 结晶（各 50%） |
| 素材 | `/assets/小怪/史莱姆x-3.png` |

### 6.7 冰人（Boss）

| 项 | 值 |
|----|-----|
| 出场 | 存活 **≥420 秒** 刷第 1 只；死后 **180 秒** 重生（同时最多 1 只） |
| 生命 | 第 1 只 **3800**；之后 `round(3800×1.4^n)`，n=已死亡次数 |
| 绘制 | **48×48**（原 24 的 ×2；hurtbox 跟着放大） |
| 接触 | −1 心（乱晃碰到也算） |
| 击退 | 只吃 **70%**（`knockbackScale=0.3`） |
| 平时移速 | 生成时角色移速 **×0.7**（快照；本环不减） |
| 移动 | 地图乱晃，尽量留在视野；出视野则靠回。不主动贴脸 |
| 逃跑 | 累计 400 伤害 → 远离 10 秒，移速 = 角色当前移速 +0.2；不跑出视野 |
| 弹幕 | 每 **17** 秒一套；视野黑边四角/十字 4 种随机排序；`怪物子弹.png`；速度 **200**；每发 −1 心 |
| 冲刺 | 每 **12** 秒 1～6 次；0.25s 预警；锁定方向直线 ×3.2；每下 **8 身位**，跑完再开始下一下；撞到 −1 心 |
| 召唤 | 每 **32** 秒 **3** 只兰花 |
| 脱战回血 | 周围 2 身位无角色持续 **3 秒** → 每 1 秒回 **20**（绿边数字 `#3dbf5a`，与草地绿不同）；进范围即停并重置；重生等待期不回 |
| 掉落 | **150** 结晶（每个 **10% 独立概率**为高级结晶） |
| 素材 | `/assets/小怪/冰人.png` |

### 6.8 兰花

| 项 | 值 |
|----|-----|
| 出场 | 冰人每 32 秒刷 **3** 只 |
| 生命 | **1**（固定） |
| 接触 | 无伤害 |
| 移速 | **1.17**，无成长 |
| 行为 | 走向血量较少的活怪 |
| 回血 | 每 15 秒，周围 3 身位 **+10**（绿边数字）。每成功回血 2 次：范围内其他活怪甲 +1 |
| 素材 | `/assets/小怪/兰花.png` |

### 6.9 蝎子怪

| 项 | 值 |
|----|-----|
| 出场 | 存活 **≥300 秒**；成长从 300s 起算 |
| 生命 | **105 + 15×floor((秒−300)/45)** |
| 绘制 | 32×32 单帧 |
| 接触 | −1 心；可击退；减免 0 |
| 刷怪 | **每 20 秒** 一波；每波 **2 + floor((秒−300)/30)** |
| 移速 | **0.8 + 0.05×floor((秒−300)/45)** 设计单位（无 1.4 帽） |
| 掉落 | 2 结晶 |
| 行为 | 距玩家 >8 身位则靠近；3～8 身位每 5s 射一发（`怪物子弹.png`，速度 180，−1 心）；<3 身位背离跑到 7 身位。**每只独立 CD**：首次开火随机 0～5 秒错峰，之后各自 5 秒节奏 |
| 素材 | `/assets/小怪/蝎子怪.png` |

### 6.10 毒刺怪

| 项 | 值 |
|----|-----|
| 出场 | 存活 **≥300 秒** 起**蘑菇怪停刷**（场上存量保留至死亡），刷怪位由毒刺怪接管；每 **5 秒**一波 `6 + floor(秒/180)` 只 |
| 生命 | `260 + 20×floor((秒−300)/45)`；难度二每阶再 **+5**（走 `getHpGrowthAdd`） |
| 移速 | `0.70 + 0.02×floor((秒−300)/45)` 设计单位，**无帽**（不套 1.4） |
| 击退抗性 | **1 身位**（`knockbackResist = BODY`，同蜗牛：每次击退距离 −1 身位，最低 0） |
| 掉落 | **4** 结晶，每个 **20% 独立概率**为高级结晶 |
| 接触 | −1 心 |
| 素材 | `/assets/小怪/毒刺怪.png` |

---

## 7. 难度成长一览

难度二：蘑菇 / 蜗牛 / 裂怪 / 史莱姆x-1 / 普通树的「每阶 +N 血」**N+5**。灰树百分比、冰人、兰花不加。

难度一：

| 对象 | 数量/波 | 间隔 | 移速 | 生命 |
|------|---------|------|------|------|
| 蘑菇怪 | `5+floor(秒/120)`（300s 起停刷） | 5 | `(0.62+0.02t)×80`，帽 1.4 | `32+10t` |
| 蜗牛怪（≥120s） | `4+floor((秒−120)/45)` | 5 | `(0.62+0.01u)×80`，无 1.4 帽 | `50+20×floor((秒−120)/40)`；出生甲 1 |
| 灰树 | 2（需 ≥30s） | `max(2,5×0.94^t)` | — | `80×(1+0.15t)`；自损 1～(8+满分钟) |
| 普通树 | 1 | `max(5,10×0.97^t)` | — | `50+5t` |
| 裂怪 | 毁后 +1s ×4 | — | `(0.77+0.02t)×80`，帽 1.4 | 同蘑菇怪 |
| 史莱姆x-1（≥180s） | `6+floor((秒−180)/45)` | 15 | `(0.87+0.01u)×80`，无帽 | `120+22×floor(u/45)` |
| 史莱姆x-3 | x-1 死 ×3 | — | 母体死时移速 +0.10 | `ceil(母体生成血×0.25)`；出生 0.3s 无敌 |
| 蝎子怪（≥300s） | `2+floor((秒−300)/30)` | 20 | `(0.8+0.05u)×80`，无帽 | `105+15×floor(u/45)` |
| 毒刺怪（≥300s） | `6+floor(秒/180)` | 5 | `(0.70+0.02u)×80`，无帽 | `260+20×floor(u/45)`；难度二 +5；掉 4 结晶每个 20% 高级；击退抗性 1 身位 |
| 冰人（≥420s） | 1；死后 180s 重生 | — | 生成时×0.7；逃跑当前+0.2 | 3800 起，重生 ×1.4；脱战 3s 每秒回 20；击退×0.3；掉 150 结晶（每个 10% 高级）；冲刺 12s；弹幕 17s；兰花 32s |
| 兰花 | 冰人 32s ×3 | — | 1.17 | 1；回血 10；每 2 次回血给范围内其他怪 +1 甲 |

---

## 8. 地图 / 镜头 / 素材

| 项 | 值 |
|----|-----|
| 地图 | **11000×11000**（22×500） |
| 视野 | 400×225；整数倍像素缩放 |
| 镜头 | 跟随主角 |
| 运行时资源 | `frontend/public/assets/` |
| 仓库备份 | `assets/source/` |
| 角色源 | `characters/1/` 游侠、`2/` 战士、`3/` 法师 + `Other/` |
| 子弹 | `子弹/法球.png`、挥砍、`强化箭矢x.png`（激光）、`怪物子弹.png`（冰人/蝎子）；伤害数字 `游戏数字/tile_0–9.png` |
| BGM | `游戏音乐/music.ogg`（循环；音量滑条） |
| 旧桌面源 | `C:\Users\user\Desktop\Roger-png\` |

程序生成：火柴人兜底、结晶、水果、草地、蓄力条。升级图标为过审 PNG。

---

## 9. 后端统计

| 接口 | 说明 |
|------|------|
| `GET /api/health` | 健康检查 |
| `POST /api/matches/start` | 开局 |
| `POST /api/matches/{id}/end` | 结束上报 |
| `POST /api/matches` | 无 id 一次性上报 |
| `GET /api/matches/top` | 排行 |

字段：`survivedSec`、`kills`、`level`、`exp`、`difficulty`。  
API：`http://localhost:8080/api`。

---

## 10. 已知限制

| 项 | 说明 |
|----|------|
| 升级图标 | **击退 / 伴我同行** 目前局内空白（空白方块占位）；其余升级选项均有已过审 PNG |

---

*改数值请先改对应 constants，再回写本表。*
