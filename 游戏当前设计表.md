# 类幸存者 — 游戏当前设计表

> **对齐基准**：以当前代码常量为准；与旧文档冲突时以代码为准。  
> **主要来源**：`frontend/src/game/**`、`frontend/src/ui/**`、`frontend/src/game/match.js`；规格 `docs/GAME-SPEC.md`。  
> **对齐日期**：2026-08-18（P11 已入局：跟班独立索敌、回忆悬停、怪物数值、力量+10、每5级成长）

---

## 1. 产品概览

| 项 | 当前实现 |
|----|----------|
| 游戏名 | **类幸存者** |
| 类型 | 网页端、类吸血鬼幸存者、2D 俯视像素 |
| 叙事 | 暂无剧情背景 |
| 视觉 | 浅绿简约；草地色板对齐浅树 |
| 技术 | Vue 3 + Vite + Canvas；Spring Boot 3 统计局数据 |
| 可玩角色 | **游侠**（其余「敬请期待」） |
| 可玩武器 | **游侠弓**（无选武器页） |
| 难度 | 仅 **难度一**（`id: "1"`） |
| 通关 | 存活 **600 秒（10 分钟）** → 强制胜利 |
| 失败 | 生命归零 → 结算 |

---

## 2. 操作与 UI 流程

### 2.1 阶段

| phase | 含义 |
|-------|------|
| `menu` / `char` / `difficulty` | 主页 → 选角 → 选难度 |
| `settings` | 设置（菜单进入，或局内暂停） |
| `playing` / `upgrade` | 局内 / 升级三选一 |
| `result` / `victory` | 死亡结算 / 通关 |

### 2.2 操作

| 输入 | 效果 |
|------|------|
| WASD | 移动 |
| 鼠标 | 瞄准；**按住蓄力、松开发射** |
| 齿轮（场景外右上）或 ESC | 局内暂停并打开设置 |
| ……（场景外左下） | 打开更多；其中「回忆」查看最近 10 局 |
| ESC（设置中） | 关闭设置并恢复 |

### 2.3 设置

| 项 | 默认 | 说明 |
|----|------|------|
| 音量 | 0.7 | 滑条实时；**0 = 静音**；持久化 localStorage |
| 返回主页 | — | 局内需确认：「返回后本局无法保持，是否退出？」 |
| 测试模式 | 关 | 红=打开 / 绿=关闭 |
| └ 无敌 | 关 | 血量最低 1 |
| └ 无限弹药 | 关 | **蓄力上限视为 0**（无需蓄力，满伤射出） |
| └ 刷怪速度 | 1 | 0.25～2，步进 0.25。**仅测试模式**。与当前时间该种怪的每波数量相乘（四舍五入，至少 1） |
| └ 提高等级 | 0 | 仅测试模式；**0～3**（提高当前等级，不是跳到某级）。**关掉设置后**先播 +1，再弹出三选一，最多 3 次 |

### 2.4 升级三选一（从池中随机抽 3）

图标：已过审 PNG 用图。升 1 级时先出现角色旁 **+1**，播完再出三选一。  
普通池日常抽 3 项。**每第 5、10、15… 次**三选一有 **30%** 将其中 1 格换成高级项（本环：地精）。

| ID | 名称 | 效果 |
|----|------|------|
| `move_speed` | **敏捷** | 移速 +0.20（设计单位） |
| `ammo_cap` | **散射** | 弹道 +1，伤害 −3（可叠） |
| `reload` | **技巧** | 蓄力时间 −0.20s；减到 ≤0 后从池中移除 |
| `power` | **力量** | 攻击 +10 |
| `survive` | **生存** | 血上限 +1，当前血 +1（2 滴 → 3/4，不会加满） |
| `recover` | **恢复** | 回复 2 滴血（不超过上限） |
| `earth` | **大地啊** | 普通树每波 +2，果实概率 +10% |
| `pierce` | **穿透** | 箭矢穿透 +1（未蓄/满蓄都加；可叠） |
| `eyes` | **开眼了** | 背后弹道 +1，伤害 −3（叠法同散射） |
| `giant` | **大娃** | 首次弹体与碰撞 ×2，之后每次 +50% |
| `blackhole` | **黑洞** | 全图经验结晶以吸入速度飞向角色（水果不吸） |
| `magnet` | **磁铁** | 结晶吸取范围 +50%（相对当前，可叠） |
| `goblin` | **地精** | **高级 / 跟班**。生成 1 个地精，**所有跟班伤害 +10**（可叠）。碰撞重叠才打；0.4s；伤 ceil(15+攻击×0.6)+加成；两敌彼此重叠才打 2 个。图标左上 **红点** |
| `rabbit` | **兔子** | **普通 / 跟班**。生成 1 个兔子。碰撞才打；0.4s；伤 20+跟班加成；最多 1 个敌人。独立索敌，从怪朝角色一侧切入 |

回忆里查看对局：图标从左到右，图标之间留 `×000` 宽；次数为 1 也显示 ×1。鼠标对准图标显示该选项效果。高级选项图标左上 3×3 红点。

### 2.5 跟班

独立属性。无血、无击退。移速 = 选择该跟班时角色当前速度。每只跟班独立锁定不同敌人，走进重叠才造成伤害。兔子从怪朝角色的一侧切入，不跟在怪身后空追。没敌人则跟角色。多只跟班不要叠成一张图。地精贴图 `跟班/地精.png`，兔子 `跟班/兔子.png`。碰撞重叠才造成伤害。

---

## 3. 主角「游侠」

| 项 | 值 |
|----|-----|
| 尺寸 | 宽 **11**、高 **22** |
| 贴图 | `characters/1/` D/S/U Idle/Walk/Attack/Hurt/Death；脚下 Shadow |
| 选角立绘 | 侧向 Idle 第 0 帧，**朝右** |
| 生命 | **3**；受伤 −1；无敌约 **0.85s** + 像素飞散 |
| 移速 | **1.20** 单位 × 80 = **96** px/s |
| 等级成长 | 每升 **5** 级（到 6 / 11 / 16…）：攻击 **+1**，移速 **+0.05** |
| 接触 hurtbox | 小于绘制约 `0.55×0.45` |

身位 `BODY=22`（= 身高），用于击退、吸附等。无贴图时火柴人兜底。

---

## 4. 武器「游侠弓」

| 项 | 值 |
|----|-----|
| 弹匣 / 换弹 | **无** |
| 蓄力 | 上限 **0.75s**；按住蓄、松开发 |
| 射速 CD | **0.21s** |
| 攻击 | 独立属性，初值 **20** |
| 伤害 | 未蓄 = 攻击；满蓄 = 攻击 ×2；中间线性 |
| 击退 | **1 → 2** 身位；实体可减免；**最低 0** |
| 穿透 | 仅满蓄基础 +1；升级「穿透」每次再 +1 |
| 弹速 | **520** |
| 素材 | `/assets/characters/Other/Arrow.png`；击中 D/S/U `_Blood.png` |

---

## 5. 战斗与掉落

### 5.1 经验

| 项 | 值 |
|----|-----|
| 需求 | `15 + 4×(level−1)`（升到下一级） |
| 上限 | **无** |
| 来源 | **拾取结晶**（1 结晶 = 1 经验） |
| 升级表现 | 每升 1 级，角色周围出现程序绘制的 **+1**（上浮消失）；**播完后**才出现三选一。自然捡结晶与测试提高一样要停顿。升级期间结晶可飞向角色，**不拾取、不加经验** |

### 5.2 掉落

| 来源 | 掉落 |
|------|------|
| 蘑菇怪 / 蜗牛怪 | 1 结晶 |
| 史莱姆x-1 | 2 结晶 |
| 史莱姆x-3 | 0 或 1 结晶 |
| 普通树 | 结晶 **3～(6+2×满分钟数)**；水果 **30%**（+1 心）。0 分 3～6，1 分 3～8，以此类推 |
| 吸附 | ≤ **2 身位** 自动吸入；磁铁每次当前范围 ×1.5 |
| 吸入速度 | **135** px/s |

---

## 6. 怪物与危险物

刷怪环：**视野外** 且距玩家 ≤ **1000**。  
蘑菇怪 / 灰树难度阶：`t = floor(存活秒 / 45)`。  
加新怪物：填根目录 **怪物属性表.md**。  
蘑菇怪移速：`px/s = 80 × min(1.4, base + 0.08t)`，**不乘**玩家当前移速。  
击退：`max(0, 原始 − 减免)`。

### 6.1 蘑菇怪

| 项 | 值 |
|----|-----|
| 生命 | **27 + 5t** |
| 绘制 | 32×32 单帧，缩放到约 16～32 |
| 接触 | −1 心；缩小 hurtbox |
| 可击退 | 是 |
| 刷怪 | **每 5 秒** 一波；每波 **5 + floor(秒/120)**（2 分钟 6 只，持续 +1） |
| 移速 base | **0.75**（t=0 为 60 px/s） |
| 素材 | `/assets/小怪/蘑菇怪.png` |

### 6.2 蜗牛怪

| 项 | 值 |
|----|-----|
| 出场 | 存活 **≥120 秒**；成长从 120s 起算 |
| 生命 | **45 + 10×floor((秒−120)/40)** |
| 绘制 | 32×32 单帧 |
| 接触 | −1 心 |
| 击退 | 减免 **1 身位**（最低 0） |
| 刷怪 | **每 5 秒** 一波；每波 **4 + floor((秒−120)/45)** |
| 移速 | **0.75 + 0.07×floor((秒−120)/45)** 设计单位（无 1.4 帽） |
| 素材 | `/assets/小怪/蜗牛怪.png` |

### 6.3 灰树

| 项 | 值 |
|----|-----|
| 生命 | `25 × (1 + 0.15t)`（t=0 为 25） |
| 可击退 | **否** |
| 子弹 | 可打 |
| 撞玩家 | −1 心并**立即销毁** |
| 出场 | 存活 **≥30 秒** 后才刷；解锁瞬间先刷 1 波 |
| 每波 | **固定 2 棵** |
| 间隔 | `max(2.0, 5 × 0.94^t)` |
| 自损 | **每棵独立**：每 **3 秒** 随机扣 **1～5** |
| 毁坏 | **1 秒后**原位生成 **4** 只裂怪（贴图 `/assets/小怪/裂怪.png`），绕爆点隔开 |
| 裂怪移速 base | **0.9**（t=0 为 72 px/s） |
| 素材 | `/assets/树木/灰树.png` |

### 6.4 普通树

| 项 | 值 |
|----|-----|
| 生命 | `50 + 5t` |
| 销毁 | **仅子弹**；碰撞只挡 |
| 开局 | 视野外 **3** 棵 |
| 刷取 | 每波 1；间隔 `max(5, 10×0.97^t)` |
| 掉落 | 结晶 **3～(6+2×满分钟数)**；果 30% |
| 素材 | `/assets/树木/浅树.png` |

### 6.5 史莱姆x-1

| 项 | 值 |
|----|-----|
| 出场 | 存活 **≥300 秒**；成长从 300s 起算 |
| 生命 | **85 + 12×floor((秒−300)/45)** |
| 绘制 | 32×32 单帧 |
| 接触 | −1 心；可击退；减免 0 |
| 刷怪 | **每 15 秒** 一波；每波 **6 + floor((秒−300)/45)** |
| 移速 | **1.00 + 0.07×floor((秒−300)/45)** 设计单位（无 1.4 帽） |
| 掉落 | 2 结晶 |
| 死亡 | 原地立刻 **3** 只史莱姆x-3 |
| 素材 | `/assets/小怪/史莱姆x-1.png` |

### 6.6 史莱姆x-3

| 项 | 值 |
|----|-----|
| 出场 | 不独立刷；x-1 死亡时原地 ×3 |
| 生命 | `ceil(该 x-1 生成时血量 × 0.25)` |
| 移速 | x-1 死亡当时移速 **+0.10**（快照） |
| 接触 | −1 心；可击退；减免 0 |
| 出生 | **0.3 秒**无敌（子弹/跟班打不中，也不伤玩家） |
| 掉落 | 0 或 1 结晶（各 50%） |
| 素材 | `/assets/小怪/史莱姆x-3.png` |

---

## 7. 难度一成长一览

| 对象 | 数量/波 | 间隔 | 移速 | 生命 |
|------|---------|------|------|------|
| 蘑菇怪 | `5+floor(秒/120)` | 5 | `(0.75+0.08t)×80`，帽 1.4 | `27+5t` |
| 蜗牛怪（≥120s） | `4+floor((秒−120)/45)` | 5 | `(0.75+0.07u)×80`，无 1.4 帽 | `45+10×floor((秒−120)/40)` |
| 灰树 | 2（需 ≥30s） | `max(2,5×0.94^t)` | — | `25×(1+0.15t)` |
| 普通树 | 1 | `max(5,10×0.97^t)` | — | `50+5t` |
| 裂怪 | 毁后 +1s ×4 | — | `(0.9+0.08t)×80`，帽 1.4 | 同蘑菇怪 |
| 史莱姆x-1（≥300s） | `6+floor((秒−300)/45)` | 15 | `(1.00+0.07u)×80`，无帽 | `85+12×floor(u/45)` |
| 史莱姆x-3 | x-1 死 ×3 | — | 母体死时移速 +0.10 | `ceil(母体生成血×0.25)`；出生 0.3s 无敌 |

---

## 8. 地图 / 镜头 / 素材

| 项 | 值 |
|----|-----|
| 地图 | **11000×11000**（22×500） |
| 视野 | 400×225；整数倍像素缩放 |
| 镜头 | 跟随主角 |
| 运行时资源 | `frontend/public/assets/` |
| 仓库备份 | `assets/source/` |
| 角色源 | Craftpix kit → `characters/1/` + `Other/` |
| BGM | `游戏音乐/music.ogg`（循环；音量滑条） |
| 旧桌面源 | `C:\Users\user\Desktop\Roger-png\` |

程序生成：火柴人兜底、结晶、水果、草地、蓄力条。升级图标为过审 PNG。

---

## 9. 后端统计

| 接口 | 说明 |
|------|------|
| `GET /api/health` | 健康检查 |
| `POST /api/matches/start` | 开局 |
| `POST /api/matches/{id}/end` | 结束上报 |
| `POST /api/matches` | 无 id 一次性上报 |
| `GET /api/matches/top` | 排行 |

字段：`survivedSec`、`kills`、`level`、`exp`、`difficulty`。  
API：`http://localhost:8080/api`。

---

## 10. 已知限制

| 项 | 说明 |
|----|------|
| 音量 | 滑条即时改循环 BGM；拉到 **0%** 静音 |
| 灰树数量 | 代码每波固定 2（未启用 6 帽成长） |
| 升级 / 设置暂停 | `playing` 以外整帧停（敌人/玩家/跟班皆停；升级 +1 仍会飘） |
| 测试刷怪速度 | 仅测试模式；乘在当前时间的每波数量上 |
| 地精升级图标 | 现用跟班立绘缩小（`upgrades/goblin.png`）；不是 12×12 手绘 |
| Fireball.png | 已拷入 `Other/`，本阶段未使用 |

---

*改数值请先改对应 constants，再回写本表。*
